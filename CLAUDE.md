# CLAUDE.md — FinTrace

Tài liệu này là **điểm vào duy nhất** cho mọi session Claude làm việc trên repo. Đọc hết trước khi làm gì.
Nó ghi tinh thần làm việc của giai đoạn hiện tại, các quyết định đã chốt, và phạm vi của pha đang mở.
`SPEC.md` vẫn là đặc tả gốc của 5 tab tài chính (Sổ tháng, Công nợ, Tài sản, Lịch sử, Tổng quan); khi hai file mâu thuẫn thì **file này thắng**.
`HUONGDAN.md` là **tài liệu cho người dùng cuối** (owner và trợ lý ghi thu chi), viết theo thao tác chứ không theo code — đổi UI của tab Dòng tiền, tên nhóm chi/thu, hay quy ước nhập liệu thì **sửa file đó cùng lúc**. App có link tới nó ở Cài đặt → Tài khoản.

---

## 1. Tinh thần làm việc: vibe coding, code trước

- **Code trước, bàn sau.** Khi yêu cầu đủ rõ để bắt tay được thì làm ngay, chọn mặc định hợp lý thay vì hỏi. Chỉ dừng lại hỏi khi hai cách hiểu dẫn đến hai sản phẩm khác hẳn nhau, hoặc khi thao tác không quay lại được (xoá dữ liệu thật, đổi Firebase project).
- **Owner kiểm tra bằng cách dùng, không phải bằng cách đọc code.** Mọi thay đổi phải nhìn thấy và bấm được trên GitHub Pages sau khi merge. Không có "làm xong nhưng chưa nối vào UI".
- **Mỗi session một việc, kết thúc bằng merge lên `main`.** Quy trình chuẩn cho session làm code:
  1. Làm trên nhánh của session (`claude/...`).
  2. Tự kiểm tra (mục 3).
  3. Commit với message tiếng Việt ngắn gọn, nói *cái gì đổi* chứ không phải *cách đổi*.
  4. Mở PR, mô tả ngắn: đổi gì, owner mở tab nào để thấy, cần làm gì thêm trên Firebase console (nếu có).
  5. **Merge ngay vào `main`** (không chờ review). GitHub Pages tự deploy trong 1–2 phút.
  6. Báo owner: link PR, link app, và đúng 1 đoạn "mở app, vào tab X, thấy Y".
- **Toàn bộ trên cloud.** Owner không chạy terminal, không clone, không `npm`. Mọi hướng dẫn cho owner chỉ được là thao tác trên trình duyệt: GitHub, Firebase console, Binance, và chính app. Nếu một việc bắt buộc phải chạy lệnh thì Claude chạy trong session, không giao cho owner.
- **Nhỏ và chạy được, hơn là đủ mà chưa chạy.** Chia pha nhỏ, mỗi pha dùng thật vài ngày rồi mới mở pha sau. Không xây trước những thứ "sau này sẽ cần".
- **Không đổi kiến trúc.** Single-file `index.html`, inline CSS + JS, không framework, không build step, không bundler, không TypeScript. Thư viện ngoài chỉ qua CDN và chỉ khi thật sự cần (hiện chưa dùng gì ngoài Firebase SDK).
- **Ngôn ngữ:** UI, commit, PR, tài liệu đều tiếng Việt. Tên biến/hàm tiếng Anh như code hiện có.

---

## 2. Kiến trúc hiện có (đọc để không phá)

Mọi thứ nằm trong `index.html` (~1.900 dòng). Thứ tự trong file:

| Khối | Tìm bằng | Ghi chú |
|---|---|---|
| `CONFIG` | `const CONFIG =` | Firebase config. Trống → chế độ thử cục bộ (localStorage). |
| Hằng số | `PRICE_KEYS`, `LIVE_KEYS`, `DEFAULT_*` | priceKey có `live:true` là crypto (usdt, btc, eth, ada). |
| Store | `COLLECTIONS`, `makeLocalStore`, `makeFirestoreStore` | Hai adapter cùng API: `loadAll, set, update, remove, batch, clearAll`. **Thêm collection mới = thêm tên vào `COLLECTIONS` và vào `loadAll` của cả hai adapter.** |
| State | `const state`, `const D` | Mọi document nạp về `D` lúc đăng nhập. UI render từ `D`, ghi nền qua `bg()`. |
| Calc | `monthTotals`, `debtTotals`, `netWorth`, `attribution`, ... | Hàm thuần, tính từ `D`. Tiền là **nghìn đồng**. |
| Mutation | `addTx`, `addHolding`, `addTransfer`, `setPrice`, `saveSettings`, `closeMonth` | Mẫu chuẩn: sửa `D` → `render()` → `bg(store.set(...))`. |
| Router | `VIEWS`, `VIEW_ALIAS`, `route()`, `render()` | Hash routing `#tong #dongtien #taisan #dca #chungkhoan #trade #caidat` (`#lichsu` cũ dẫn về `#tong`). `VIEW_ALIAS` giữ hash cũ `#so` và `#congno` chạy được. `render()` gọi đúng một `renderXxx` theo `state.view`. |
| Render | `renderOverview`, `renderCashflow`, `renderHoldings`, `renderDca`, `renderTrade`, `renderStocks`, `renderSettings` | Mỗi tab một hàm, dựng innerHTML bằng template string, `esc()` mọi chuỗi người dùng. `renderCashflow` gọi `renderLedger` + `renderDebts`. |
| Import | `importInitialData` | Chạy một lần, dữ liệu mục 6 SPEC.md. |

**Thêm hoặc gộp tab** cần đúng 5 chỗ: một `<a data-view>` trong `nav.nav` (sidebar) và trong `nav.tabbar` (mobile), một `<section id="view-xxx" class="view">`, tên trong `VIEWS`, và `renderXxx` trong map của `render()`. Cộng thêm số cột của `.tabbar` (`grid-template-columns:repeat(N,1fr)`, N = số tab) và `VIEW_ALIAS` nếu bỏ một hash cũ. **`testViews()` kiểm tra đủ sáu chỗ này** — chạy nó sau mọi thay đổi điều hướng.

Dữ liệu đã có sẵn và **phải dùng lại** cho DCA:
- `holdings`: có ETH (priceKey `eth`) và USDT (priceKey `usdt`), giá trị = quantity × price, quy về nghìn đồng.
- `transfers`: mỗi lần đổi USDT → ETH là một document `{date, fromId, toId, qtyOut, qtyIn, valueVnd, note}`. `transferImplied()` cho giá ngầm định. Đây chính là lịch sử mua ETH.
- `prices/current`: map priceKey → `{value, source, updatedAt}`, nghìn đồng cho 1 đơn vị. `usdt = (USD/VND + 400)/1000`, `eth = giá USD × usdt`.

---

## 3. Tự kiểm tra trước khi merge

Không có test runner. Tối thiểu mỗi PR làm trong session:

1. Mở `index.html` bằng Chromium có sẵn (Playwright, `executablePath: '/opt/pw-browsers/chromium'`) với `CONFIG` trống → app vào chế độ thử cục bộ, **không có lỗi trong console**.
2. Bấm qua tất cả các tab, kể cả tab vừa thêm, ở cả viewport desktop (1200px) và điện thoại (390px).
3. Gõ `testAttribution()` trong console → phải ra `25 đạt · 0 hỏng`. Nếu thêm hàm tính mới cho DCA thì thêm `testDca()` tương tự: vài ca cố định, in "đạt/hỏng".
4. Chụp màn hình tab vừa làm, gửi cho owner trong phần báo cáo.

Không merge khi console còn lỗi. Không "sửa sau".

---

## 4. Các quyết định đã chốt (2026-09-12)

1. **Repo `coin` và CoinDCA bỏ.** File `SHARED_PRODUCT_ROADMAP.md` đã xoá. FinTrace là sản phẩm duy nhất; DCA là **một tab nhẹ bên trong FinTrace**, không phải engine riêng.
2. **Tài liệu "ETH DCA Operating System V1.0"** là nguồn tham khảo công thức (Buy Score mục 6, cấu trúc vốn mục 8–11, giá vốn mục 24). Không áp kiến trúc backend/Postgres/cron/state machine của tài liệu đó vào repo này.
3. **Dữ liệu 100% trên Firebase, project mới hoàn toàn.** Owner tạo project theo `SETUP.md`, điền `CONFIG` vào `index.html` (apiKey Firebase không phải bí mật, commit được). Không dùng lại project cũ. Sau khi nối, chạy Import dữ liệu ban đầu một lần.
4. **Nguồn dữ liệu thị trường: Binance trước, CoinGecko dự phòng.** Thử Binance trực tiếp từ trình duyệt; chỉ chuyển sang CoinGecko (hoặc Cloudflare Worker) khi Binance không gọi được từ GitHub Pages.
5. **SPEC.md mục 1 ghi "kịch bản DCA" ngoài phạm vi** — dòng đó hết hiệu lực. Các phần còn lại của SPEC.md giữ nguyên.

---

## 5. Pha đang mở: DCA P1 — tab "DCA"

Mục tiêu một câu: mở tab DCA, trong 5 giây biết **ETH đang bao nhiêu, Buy Score bao nhiêu, tháng này còn bao nhiêu vốn phải/được mua, và mình đang giữ bao nhiêu ETH với giá vốn nào**.

Làm theo thứ tự **Khối C → Khối A → Khối B**, mỗi khối một PR, merge xong mới làm khối sau.

### Khối C — Lịch sử mua và giá vốn (chỉ dùng dữ liệu đã có)
- Thêm trường tuỳ chọn `source` vào `transfers`: `BASE | SMART | OPPORTUNITY | MANUAL`. Form chuyển đổi hiện thêm select này **chỉ khi** holding nhận có priceKey `eth`. Mặc định `MANUAL`.
- Tab DCA hiện: Tổng ETH, Tổng vốn (nghìn đồng), Giá vốn trung bình (USD và nghìn đồng), Giá hiện tại, Giá trị hiện tại, Lãi/lỗ và %. Ưu tiên ETH tích luỹ và giá vốn hơn PnL.
- Bảng lịch sử: Ngày, Giá ETH (USD ngầm định), Số tiền, ETH nhận, Nguồn, Buy Score lúc mua (nếu có), Giá vốn TB sau lần mua. Lọc theo nguồn.
- Giá vốn tính từ `transfers` có `toId` là holding ETH; USD = `valueVnd / usdt tại thời điểm đó`. Nếu chưa lưu tỷ giá lúc mua thì dùng `usdt` hiện tại và ghi rõ "ước tính".

### Khối A — Buy Score và chỉ báo (chỉ đọc, tính lúc mở tab)
- Nút "Cập nhật" gọi Binance public REST (không cần key): `GET /api/v3/klines?symbol=ETHUSDT&interval=1d&limit=400` và `BTCUSDT` tương tự; `GET /api/v3/ticker/price` cho giá hiện tại. Kline có close và volume, đủ cho mọi chỉ báo.
- Tính 7 thành phần đúng bảng điểm mục 6 của tài liệu: Drawdown365 /25, MA200 /20, RSI14 (Wilder) /15, Monthly drawdown /15, Percentile365 /10, Capitulation (7D return + volume ratio) /10, ETH/BTC 30D /5. Tổng clamp 0–100, Market State theo mục 6.8, Confidence theo mục 7.
- Tất cả là **hàm thuần** nhận mảng nến, trả object `{score, components, marketState, confidence}`. Ngưỡng và trọng số để trong một object hằng `BUY_SCORE_V1` ở đầu file, không rải trong code.
- Hiện: hero (giá ETH, Buy Score, Market State, Confidence, thời điểm dữ liệu) và bảng breakdown 7 dòng dạng `x/25`.
- Cache kết quả lần gần nhất vào `prices/current` dưới khoá `dca` (kèm `updatedAt`). Quá 48 giờ → nhãn "dữ liệu cũ". Không lưu lịch sử snapshot.
- Cập nhật DCA cũng cập nhật luôn giá `btc`, `eth` trong bảng giá (source `live`) → bật nút "Cập nhật giá live" ở tab Tài sản đang disabled. `usdt` vẫn nhập tay cho đến khi có nguồn tỷ giá.
- Nếu Binance lỗi (CORS, chặn vùng, 429): hiện toast, giữ cache, **không** ra Buy Score sai. Chỉ khi xác nhận Binance không dùng được từ Pages mới đổi sang CoinGecko `market_chart?days=365`, giữ nguyên hàm tính.

### Khối B — Kế hoạch vốn tháng (suy ra từ cài đặt, không có state riêng)
- Cài đặt thêm: ngân sách tháng (nghìn đồng), tỷ lệ Base/Smart/Opportunity (mặc định 50/30/20), lịch Base (mặc định ngày 3, 13, 23 với 40/30/30), tháng bắt đầu, cap Opportunity (mặc định 6 tháng).
- Tab DCA hiện card "Tháng này": Base đã mua / phải mua và kỳ nào đã tới hạn; Smart unlock % theo Buy Score (mục 10) và số tiền; Opportunity Fund số dư, cap, unlock % theo mục 11 và số tiền.
- Opportunity Fund **không có ledger**: số dư = số tháng từ tháng bắt đầu × đóng góp tháng − Σ transfers `source = OPPORTUNITY`, chặn ở cap. Cùng cách "suy ra từ dữ liệu gốc" như công nợ ròng.
- Một dòng gợi ý tĩnh, ví dụ: "Score 72 · Smart mở 100% (3.000k) · Base kỳ 13 chưa mua (1.500k)". Không phải lệnh, không có trạng thái.

### Đồng bộ tài sản từ Binance — ❌ ĐÃ GỠ, ĐỪNG LÀM LẠI
Thử hai lần, hỏng vì hai bức tường khác nhau của Binance, cả hai đều không sửa được từ phía mình:
1. **Ký thẳng trong trình duyệt** → Binance chặn CORS với endpoint đã ký (cố ý: không muốn secret nằm trong trang web).
2. **Qua Cloudflare Worker** → Binance trả HTTP 403 kèm trang HTML của WAF, chặn theo dải IP trung tâm dữ liệu.

Owner đã chốt gỡ bỏ 2026-09-13. `binance-worker.js` và `BINANCE.md` đã xoá; nút Đồng bộ, mục cài đặt khoá, `binanceDiff`, `syncBinance`, `testBinance` đã gỡ khỏi `index.html`. `normalizeSettings` xoá luôn khoá `binance` để lần lưu cài đặt tới gỡ khoá API cũ khỏi Firestore.

**Đường duy nhất còn khả thi** (chưa làm, chỉ ghi lại): serverless **chọn được vùng** ở châu Á — Vercel `sin1`, Google Cloud Run `asia-southeast1` — hoặc một máy có IP dân dụng. Đổi lại là một thứ nữa phải nuôi. Với vài lần nhập tay mỗi tháng thì không đáng. Chỉ mở lại khi owner yêu cầu rõ.

Endpoint **công khai** của Binance (klines, ticker) vẫn gọi thẳng từ trình duyệt bình thường — Buy Score và giá live chạy tốt, không đụng gì tới phần đã gỡ.

### Quy đổi USD và WBETH (thêm 2026-09-13, owner yêu cầu)
- Nhập tay chỉ nhập **số lượng**; giá trị nghìn đồng và quy đổi USD app tự tính. Bảng Danh mục hiện `≈ $…` dưới cột Giá trị cho mọi tài sản có khoá giá live; Bảng giá có cột **USD** riêng; đầu Danh mục có tổng `crypto ≈ $…`.
- Chưa có giá `usdt` thì mọi chỗ quy đổi trả `null` và **không hiện gì** — không bao giờ đoán tỷ giá.
- `wbeth` là một khoá giá live như các khoá khác. Binance có thể không niêm yết cặp `WBETHUSDT`, nên `LIVE_SYMBOLS` cho khai cặp dự phòng `via` (`WBETHETH` × giá ETH). `resolveLiveUsd` là hàm thuần ghép hai nguồn đó.
- Giá live hỏi **từng mã một** chứ không gộp một lệnh: Binance trả lỗi cho cả lô nếu chỉ một mã không tồn tại. Mã nào hỏng thì bỏ mã đó, toast nói rõ mã nào không lấy được.

### Khối E — ETH và BTC song song (thêm 2026-09-13, owner yêu cầu)
- Tab DCA có bộ chọn coin **ETH / BTC** áp cho cả bốn phần: Buy Score, kế hoạch vốn tháng, giá vốn, lịch sử mua.
- Cùng một thuật toán, không có chiến lược riêng cho từng coin. Thành phần điểm thứ 7 soi gương nhau: ETH chấm theo ETH/BTC, BTC chấm theo BTC/ETH (`DCA_COINS[].ref`).
- Mỗi coin một khối cài đặt riêng: `settings.dca = {eth:{...}, btc:{...}}`. Cài đặt bản cũ (một khối phẳng) tự chuyển thành khối ETH qua `migrateDcaSettings`.
- Cache điểm đổi thành `prices/current.dca = {usd, coins:{eth, btc}}`; bản cache cũ vẫn đọc được cho ETH.
- Một lần bấm Cập nhật chấm điểm cả hai coin, **không** thêm lệnh gọi mạng nào (ETH và BTC vốn đã tải sẵn để tính thành phần thứ 7).
- Thêm coin thứ ba = thêm một dòng vào `DCA_COINS` và một holding có `priceKey` tương ứng. Không đụng hàm tính.

### Tab Dòng tiền (gộp 2026-09-13, owner yêu cầu)
Sổ tháng + Công nợ gộp thành một tab `#dongtien`, bốn card ngang chia đều: **Chi | Thu | Họ nợ mình | Mình nợ**.

- **Thu chi theo tháng đang xem; công nợ luôn là số dư hôm nay** (owner chốt), có nhãn `số dư hôm nay` trên card và trên ô thống kê. Lùi tháng thì cột thu/chi đổi, hai cột nợ giữ nguyên.
- Hàng thống kê 5 ô: Thu · Chi · Chênh lệch · Công nợ ròng · Mục tiêu.
- **Thu chưa nhận** trước đây hiện đủ ở cả hai tab. Giờ: khoản của tháng đang xem gộp thành một dòng tổng trong "Họ nợ mình" (đã thấy đầy đủ ở cột Thu ngay cạnh), khoản của tháng khác vẫn liệt kê từng dòng vì không nhìn thấy ở đâu khác.
- **Nhóm chi "Trả nợ" giữ nguyên, chỉ là ghi chú** (owner chốt). Trả nợ thật đi qua nút Tất toán — ghi một Chuyển đổi, tài sản ròng không đổi, không vào sổ tháng. Card "Chi theo nhóm" có một dòng nhắc khi tháng đó có chi nhóm này.
- **Ngưỡng bố cục đo bằng Playwright, không đoán:** 4 card vừa khít tới 1050px (card 193px, không bảng nào phải cuộn, không ô nhập nào tràn). Đặt ngưỡng 1040px → 2 card, 820px → 1 card. Muốn đổi bố cục thì đo lại, đừng ước lượng.
- **Card "Thống kê" + "Chi theo nhóm" (owner yêu cầu 2026-09-13):** 5 ô Thu/Chi/Chênh lệch/Công nợ ròng/Mục tiêu gộp vào một `.card.pad.statcard` (vẫn dùng chung `monthStatsHtml`/`debtTileHtml` với Tổng quan — chỉ đổi CSS trình bày trong phạm vi `.statcard`, không đụng hàm render dùng chung). Đặt trong `.grid2` cạnh card "Chi theo nhóm", **phía trên** `.ledger4`.
  - `.statcard .stats.five` dùng `flex-wrap` co giãn theo **bề rộng thật của card** (không dùng media query theo bề rộng màn hình như `.stats.five` gốc, vì card này giờ hẹp hơn cả trang).
  - "Chi theo nhóm" đổi từ bảng-mỗi-nhóm-một-thanh sang **một thanh ngang gộp theo tỉ trọng** (`.catstack`, màu theo `SHADES`, lọc nhóm 0đ giống `donutHtml()`) + chú giải dạng chip wrap (`.catlegend`) — để card này tự nhiên ngắn gọn, không cần giới hạn chiều cao bằng cuộn.
  - **Hai card này luôn bằng chiều cao khi đứng cạnh nhau**: bỏ hẳn `align-items:start`, dùng mặc định `stretch` của `.grid2`. Dưới 1100px content width, `.grid2` tự xếp dọc (không còn "cạnh nhau" nên không cần so bằng).
- `main` **không còn `max-width`** (trước là 1380px, làm màn 1920 thừa 356px bên phải). Nội dung giãn hết bề ngang màn hình ở mọi tab; đã soi cả 6 tab ở 1920px và 2560px, không tab nào tràn hay vỡ.
- **Mỗi dòng trong 4 bảng dùng icon thay cho nút chữ** (owner yêu cầu 2026-09-13): sửa (bút chì), xoá (thùng rác), đã nhận/tất toán (dấu tick trong khung, class `.tag`), mở lại (mũi tên xoay ngược). Icon SVG để trong hằng `IC_EDIT/IC_DEL/IC_CHECK/IC_UNDO`, dựng qua `rowBtn(act, icon, title, cls)`. `data-act` giữ nguyên (`edit/del/received/settle/reopen`) nên các handler click không đổi.
- **Icon thẳng cột thật (owner yêu cầu 2026-09-13, sửa lại lần đầu vì flexbox làm icon lệch theo độ dài tên):** mỗi hàng dùng **cột `<td>` thật** (`name` → `act` → `n`), không gộp vào một ô flex — trình duyệt tự tính độ rộng cột bằng nội dung rộng nhất trong cả bảng nên icon và số tiền luôn thẳng hàng dọc. `.ledger4 .tbl{table-layout:fixed}` với `.d/.act/.n` có độ rộng cố định (đo bằng Playwright: 42/66/70px), cột `name` nhận hết phần còn lại. Bên trong `td.name` có `<div class="rn">` cắt gọn bằng ellipsis (không dùng `max-width:0` trực tiếp trên `td` — đã thử, làm cả cột co về gần 0 trong bảng auto-layout, sai). Tx-row (Chi/Thu) có icon "Đã nhận" tuỳ trạng thái pending; dùng `ROW_SP` (span rỗng cùng kích thước icon) làm chỗ giữ khi vắng mặt, để sửa/xoá luôn ở đúng slot 2-3 bất kể dòng có pending hay không — nếu không có spacer này, sửa/xoá sẽ lệch cột giữa dòng pending và dòng thường.
- **Giá WBETH sai, sửa lại nguồn (owner báo 2026-09-13):** `LIVE_SYMBOLS` cũ thử cặp trực tiếp `WBETHUSDT` trước, chỉ suy qua `WBETHETH` khi cặp trực tiếp không trả về gì — nên một cặp trực tiếp thanh khoản mỏng/giá cũ vẫn thắng. Sửa: bỏ hẳn `symbol` trực tiếp của `wbeth`, luôn suy qua tỉ giá neo `WBETHETH × giá ETH`. Đồng thời đổi mọi lệnh lấy giá từ `/ticker/price` (giá khớp lệnh gần nhất, có thể cũ với cặp ít giao dịch) sang `/ticker/bookTicker` (trung bình bid/ask, luôn mới). *Chưa gọi được Binance thật để kiểm chứng số cuối cùng do sandbox chặn mạng ra ngoài — đây là sửa theo review code + kiến thức WBETH neo giá theo ETH, owner tự soi số thật sau khi lên Pages.*
- **Bảng giá hiện đơn vị USDT rõ ràng + tỉ suất quy đổi (owner yêu cầu 2026-09-13):** cột "USD" đổi tên "USDT", giá trị qua `fmtUsdt()` (hậu tố `USDT`, không lẫn với `$` của quy đổi VNĐ). Giá suy qua tỉ giá neo (như WBETH) có thêm dòng nhỏ `1 WBETH ≈ 1,0538 ETH` ngay dưới, lấy từ `rate`/`rateOf` lưu trên `D.prices[key]` lúc `refreshDca()` chạy.
- **Xem trước Chuyển đổi tài sản hiện thêm USDT tương đương (owner yêu cầu 2026-09-13):** `updateXferLabels()` nối thêm `(≈ $…)` sau giá trị nghìn đồng, dùng lại đúng `toUsd()`/`fmtUsd()` đã có ở Danh mục — không thêm quy ước hiển thị mới.
- **Giá WBETH vẫn bằng hệt giá ETH sau khi sửa nguồn — nguyên nhân thật là data, không phải fetch (owner báo lại 2026-09-13):** holding WBETH của owner được thêm **trước khi khoá giá `wbeth` tồn tại**, nên `priceKey` bị ghi tạm là `eth` — Danh mục đọc đúng `h.priceKey` nên hiện y hệt giá ETH, không liên quan gì tới việc lấy giá live đã sửa hôm trước. Bảng Danh mục **không có ô sửa `priceKey`** sau khi tạo, nên owner không tự sửa được. Thêm `wbethMismatches(holdings)` (hàm thuần, khớp `priceKey!=='wbeth'` + tên chứa "wbeth") và `fixWbethPriceKey()` chạy tự động trong `applyLoaded()` mỗi lần đăng nhập/khôi phục — tự sửa priceKey về `wbeth` và ghi lại Firestore, owner không cần làm gì thêm. Nhân tiện làm tròn cột "Giá" ở Danh mục về số nguyên (`fmtK` bỏ tham số `,4`) cho gọn — số lượng (`fmtQty`, 6 chữ số) và tỉ suất quy đổi (4 chữ số) giữ nguyên vì cần độ chính xác.
- **Cột "Giá" của Danh mục nói bằng USDT cho coin (owner yêu cầu 2026-09-13):** dòng coin (khoá giá live) đổi cột Giá thành **tổng chỗ đang giữ quy ra USDT** (dòng đậm) + **tỉ giá 1 đơn vị** ngay dưới (`1 ETH ≈ 2.452,51 USDT`). Dòng `usdt` bỏ tỉ giá vì `1 USDT ≈ 1 USDT` là thừa. Tài sản không phải coin (tiền, vàng, bạc, chứng khoán) **giữ nguyên** ô nhập giá nghìn đồng sửa tay được tại chỗ. Cột "Giá trị" bỏ dòng `≈ $…` ở đúng những dòng đã hiện USDT bên cạnh — tránh hai cột cùng một con số.
- **Vùng giá mua trong tab DCA (owner hỏi 2026-09-13: "giá nào là rẻ để mua?"):** card mới giữa hero và "Tháng này", một dòng cho mỗi Market State (≥25/45/65/80) kèm giá coin cần về, % chênh so với giá hiện tại, và Smart/Opportunity mở bao nhiêu ở đúng ngưỡng đó.
  - **Không có thuật toán mới:** `priceForScore()` gọi lại chính `buyScore()` với `price` giả định rồi **chia đôi khoảng**. Hợp lệ vì điểm giảm đơn điệu khi giá tăng — cả 5 thành phần phụ thuộc giá (giảm từ đỉnh 365/30, lệch MA200, percentile, lợi suất 7 ngày) đều vậy; RSI, khối lượng và ETH/BTC không đổi theo giá giả định nên có ngưỡng **không bao giờ tới được** (hiện đúng chữ "không tới được", không bịa số).
  - Giá trả về là mức **cao nhất còn đạt ngưỡng**, và **cắt xuống** 2 số lẻ chứ không làm tròn — làm tròn lên sẽ vượt qua đúng ngưỡng vừa dò, khiến số trên bảng không còn đạt ngưỡng ghi bên cạnh.
  - Tính một lần lúc bấm Cập nhật, lưu vào `prices/current.dca.coins[coin].bands` (4 object nhỏ). Cache cũ chưa có `bands` thì card báo "bấm Cập nhật", không cần migration.
  - Đây **không phải** ladder/buy zones của P2: không có anchor lưu lại, không có state, không có cooldown — chỉ là bảng tra cứu tính lại mỗi lần cập nhật.
- **Bảng giá thiếu tỉ giá USDT dưới mỗi dòng (owner báo lại 2026-09-13, đã bỏ sót ở lần làm trước):** lần trước chỉ thêm dòng tỉ suất neo (`1 WBETH ≈ … ETH`) cho riêng WBETH, còn tỉ giá USDT/VND dùng để quy đổi thì không hiện ở đâu cho các coin khác. Sửa: mọi dòng có giá USDT (trừ chính `usdt`) giờ có thêm dòng nhỏ `1 USDT ≈ 26,4855k` ngay dưới — dùng `fmtK(uRate,4)` (giữ 4 số lẻ, **không** dùng mặc định làm tròn nguyên như cột "Giá" vì tỉ giá quanh mức hàng chục cần số lẻ mới có nghĩa). WBETH có cả hai dòng: tỉ giá USDT chung + tỉ suất neo riêng.
- **Coin dồn hẳn về tab DCA, mua nhanh không cần rời tab (owner yêu cầu 2026-09-13):**
  - **Danh mục (tab Tài sản) không còn liệt kê từng coin.** Mọi holding có `priceKey` thuộc `LIVE_KEYS` (usdt/btc/eth/wbeth/ada) gộp thành **một dòng "Coin"** (giá trị = tổng, kèm `≈ $…`), link "xem ở DCA". Form "Thêm tài sản" ở đây bỏ nhóm `Crypto` và mọi `priceKey` live khỏi lựa chọn — thêm coin mới chỉ còn làm được ở tab DCA, tránh hai chỗ cùng sửa một dữ liệu.
  - **Tab DCA có card "Ví Coin"** (`renderDcaWallet`): bảng đầy đủ từng coin (tên/số lượng sửa tay như Danh mục cũ, giá ban đầu dùng `coinPriceCell()` — cột Giá nói bằng USDT + tỉ giá 1 đơn vị, sau tách thành hai cột và bỏ hàm này), cộng một thanh tỉ trọng (`coinAllocation()`, tái dùng đúng `.catstack`/`.catlegend` của "Chi theo nhóm") và form thêm coin mới ngay dưới. `dcaWalletTable` dùng chung `commitInline` với `holdTable`/`priceTable` (thêm vào danh sách đăng ký sự kiện) nên không viết lại logic sửa tay.
  - **Card "Mua nhanh" trong tab DCA:** form riêng (`formDcaBuy`, dựng lại mỗi lần render vào khung tĩnh `#dcaBuyBody`, sự kiện gắn delegation trên khung đó — không gắn thẳng lên `<form>` vì nó bị thay mới liên tục) — đích **luôn khoá** vào coin đang chọn (ETH hoặc BTC), không có ô chọn đích như form Chuyển đổi tài sản gốc (vẫn giữ nguyên, không đụng, ở tab Tài sản cho các nhu cầu khác). Chọn "Nguồn vốn" (Base/Smart/Opportunity/Tay) — mỗi lựa chọn ghi kèm số còn được mua tháng này lấy thẳng từ `dcaPlanNow()` — **tự điền** số lượng gửi bằng đúng phần còn lại, quy đổi qua giá của tài sản nguồn; gõ số lượng gửi thì **tự tính** số coin nhận theo giá hiện tại; cả hai đều sửa tay được sau khi tự điền. Bấm Mua gọi thẳng `addTransfer()` — cùng một hàm mà form Chuyển đổi tài sản gốc dùng, nên "Tháng này", Ví Coin, Danh mục (dòng Coin gộp), Cơ cấu và giá vốn DCA **tự cập nhật ngay**, không cần thao tác gì thêm.
  - Nếu coin đang chọn chưa có holding nào (mới thêm coin thứ ba chẳng hạn), card Mua nhanh chỉ hiện dòng nhắc thêm ở Ví Coin trước — không tự tạo holding ngầm.
- **Nạp lịch sử mua coin từ file Excel theo dõi cũ (owner cung cấp 2026-09-13):** owner tự ghi tay 29 lần mua BTC/ETH/ADA trước khi có FinTrace nên tab DCA chưa có gì để tính giá vốn. Hằng số `DCA_HISTORY_IMPORT` (trong `index.html`, cạnh `dcaState()`) chép nguyên 29 dòng `[ngày, số lượng, tổng USD, tỷ giá VNĐ/USDT]`; USD coi 1:1 với USDT; tỷ giá lấy từ đúng lần nạp USDT gần ngày mua nhất trong cùng file cũ (sai số xa nhất ~nửa tháng, dưới 0,5%).
  - **Chỉ ghi lịch sử Chuyển đổi để tính giá vốn, không đụng số lượng coin đang có.** Nút "Nạp lịch sử mua coin (file cũ)" ở Cài đặt → Nâng cao gọi `importDcaHistory()`, ghi thẳng `store.batch()` vào `transfers` — **không** gọi `addTransfer()`/`applyXfer()` vì hàm đó cộng dồn vào số lượng holding, sẽ nhân đôi số dư đang đúng (holdings đã khớp số dư thật, độc lập với sổ mua này).
  - `dcaHistoryOps(data, ids, batch)` là hàm thuần dựng danh sách document, tách riêng để test: thiếu id coin hoặc thiếu USDT thì bỏ qua dòng đó (không lỗi, không bịa). Số lượng âm trong file gốc là một lần **bán** — tự đổi chiều (from=coin, to=USDT) và **không gắn `source`** vì không phải một lần mua nên không tính vào Base/Smart/Opportunity; dòng bán này cũng tự động không lọt vào "Lịch sử mua" hay giá vốn (hàm `coinPurchases()` chỉ lọc transfer có đích là chính coin đó).
  - Đánh dấu mỗi document `historyBatch:'xlsx-2026-09'` để chặn nạp trùng — nút tự khoá (`disabled`) và đổi nhãn "Đã nạp rồi" sau khi nạp thành công, đọc lại trong `renderSettings()`.
  - **Chưa nạp** lịch sử nạp/rút USDT (Vietcombank ↔ USDT, 39 dòng khác trong cùng file) vì không cần cho giá vốn coin — chỉ mở lại nếu owner muốn có đủ audit trail dòng tiền vào sàn.
- **Giá vốn USD bị thổi lệch + WBETH chưa được tính là ETH (owner báo 2026-09-13, sổ tay ra 2143 mà app ra 2252):**
  - **Lỗi gốc:** `dcaSummary` lấy `avgCostVnd / tỷ giá USDT hôm nay`. Vốn được bỏ ra hồi tỷ giá 26.600–28.072 nhưng chia lại theo 25.756 của hôm nay ⇒ thổi giá vốn lên ~5%. Sửa: cộng USD **theo tỷ giá từng lần mua** (`transferUsd()` → `basis.totalCostUsd` → `avgCostUsd`). Đây là loại lỗi phải nhớ: **mọi con số USD lịch sử đều phải quy đổi tại thời điểm phát sinh, không bao giờ quy đổi cả cục theo tỷ giá hiện tại.**
  - **Bán ra giờ trừ vào vốn ròng**, đúng cách sổ tay owner tính: giá vốn = tiền ròng đã bỏ ra / số coin còn giữ (mức hoà vốn), không phải giá vốn kế toán bình quân gia quyền. Mua/bán trộn theo đúng thứ tự thời gian nên cột "giá vốn TB sau" của lần mua cuối bằng đúng ô tổng kết.
  - **`COIN_FAMILY = {eth:['eth','wbeth']}`** — WBETH là chính ETH đã stake, chỉ khác vỏ. `coinQtyHeld()` quy WBETH về ETH theo tỉ giá hiện tại, `coinValueHeld()` cộng cả hai ví, `coinLedger()` tách mua/bán và **bỏ qua chuyển nội bộ trong họ** (ETH → WBETH không phải bán, WBETH ← ETH không phải mua). Thêm coin có nhiều vỏ về sau = thêm một dòng vào `COIN_FAMILY`, không đụng hàm tính.
  - Ô đầu tab DCA đổi từ "Tổng ETH" (suy từ sổ mua) sang **"Đang giữ (ETH)"** = số dư thật gồm cả WBETH, dòng nhỏ ghi rõ `1,387511 ETH + 0,591049 WBETH`. Lãi/lỗ so giá trị thật của cả hai ví với vốn ròng.
  - Kiểm chứng với đúng số của owner: vốn ròng $4.356,54 · giá vốn TB **$2.142,81** (sổ tay: 2.142,805) · đang giữ 2,040686 ETH (mua ròng 2,0331 + lãi stake).
- **Dồn bố cục tab DCA + theo dõi cả ADA và USDT (owner yêu cầu 2026-09-13):**
  - **`DCA_COINS` giờ có 4 coin, chia hai hạng** bằng cờ trong chính cấu hình, không rẽ nhánh rải rác: `basisOnly` (ADA, USDT) chỉ theo dõi giá vốn — giấu hẳn hero, Vùng giá mua, Tháng này và Phân rã điểm thay vì hiện ô rỗng. `DCA_SCORED` là nhóm có Buy Score + kế hoạch vốn (ETH, BTC) và cũng chính là `DCA_COIN_KEYS`. Cài đặt chỉ có ngân sách cho nhóm scored nên `settingsCoin()` kẹp lại, chọn ADA ở tab DCA không làm mục Cài đặt đọc vào khối không tồn tại.
  - **Bộ chọn coin chuyển vào trong card "Giá vốn"** (không còn ở thanh tiêu đề), và card đó **lên hàng đầu ngang với hero**. Bỏ ô "Giá hiện tại" khỏi hàng thống kê vì hero đã có — chỉ coin `basisOnly` (không có hero) mới ghi giá hiện tại vào tiêu đề card.
  - **Vùng giá mua và Tháng này thu lại thành hai card vuông đặt cạnh nhau** (`.grid2.eq`). Vùng giá bỏ hai cột Smart/Opportunity mở (đã có nguyên vẹn ở card Tháng này ngay bên cạnh); Tháng này đổi từ `.plan3` ba cột ngang sang `.planc` ba dòng dọc, lịch Base gộp thành một dòng ngắn.
  - **Mua nhanh thành nút cạnh "Cập nhật", mở popup** thay vì card cố định. `showModal` được nới: `onOk` trả về `false` thì giữ popup mở — để số nhập sai không làm mất cả form.
  - **USDT có hai cờ riêng.** `fiatOnly`: chỉ tính lần đổi với tiền mặt/tài khoản, đem USDT đi mua coin là luân chuyển trong ví chứ không phải bán nên không kéo tỷ giá vốn đi. `pnlRate`: lãi/lỗ = số đang giữ × chênh tỷ giá, vì đã nạp 6.194 nhưng chỉ còn giữ 565 (phần kia đã thành coin) nên so với cả cục vốn đã nạp là vô nghĩa. `vndFirst`: ô giá vốn lấy nghìn đồng làm số chính và cột giá trong bảng lịch sử đổi thành tỷ giá — quy USDT ra USD thì lần nào cũng bằng 1.
  - **Nạp thêm 39 dòng nạp/rút USDT** từ cùng file cũ (`USDT_HISTORY_IMPORT`), ghi thành Vietcombank ↔ USDT. Hai mẻ có khoá riêng (`HISTORY_BATCHES`) nên ai đã nạp mẻ mua coin từ trước vẫn nạp được mẻ USDT thêm sau. Kết quả khớp sổ tay: đã nạp ròng 6.194,81 USDT · tỷ giá vốn TB 26,9545k.
  - Số kiểm chứng lại với sổ tay của owner: ETH $2.142,81 · BTC $91.314 (sổ: 91.314,4586) · ADA $0,77 (sổ: 0,7697628).
- **Tab DCA gom về hai hàng card + ba popup, kích thước card KHÔNG đổi khi đổi coin (owner yêu cầu 2026-09-13):**
  - **Bố cục 2×2 `.grid2.eq`** (hai cột bằng nhau, `align-items:stretch`): hàng 1 = hero (giá + Buy Score + **Phân rã điểm ngay dưới**) | **Giá vốn & Ví Coin gộp một card** (`#dcaBasisCard`: bộ chọn coin bên phải tiêu đề, 3 ô thống kê ngang, rồi bảng Ví Coin + thanh tỉ trọng); hàng 2 = Vùng giá mua | Tháng này. Card Phân rã điểm, Ví Coin riêng, Lịch sử mua và form Thêm coin rời **không còn**. (Bản đầu dùng `.grid3` ba cột + Ví Coin riêng — owner thấy thừa trống ở màn 1920 nên gộp lại 2026-09-13.)
  - **Ba nút ở góc trên phải: Lịch sử · Thêm coin · Mua nhanh** (cạnh Cập nhật), đều mở popup. `showModal` nhận thêm `opts {wide, noCancel}`; popup Lịch sử dùng `wide` (980px) + `noCancel`, bộ lọc nguồn nằm trong popup và chỉ hiện với coin có kế hoạch vốn. `.dialog{min-width:0}` + `#mdBody{min-width:0}` để bảng rộng cuộn ngang bên trong thay vì đẩy popup tràn màn hình điện thoại (đo: 518px → 350px ở màn 390).
  - **Nguyên tắc "đổi coin không đổi hình" = cùng số dòng, không pin pixel.** ADA/USDT **không giấu card** — hero vẫn hiện giá, điểm ghi "—". Mọi khối luôn dựng **đúng số hàng như ETH** và điền "—" chỗ không có số: `scoreTableHtml()` luôn 7 thành phần (`SCORE_PARTS`) + Tổng; Vùng giá luôn 4 ngưỡng từ `BUY_SCORE_V1.states`; Tháng này luôn 3 dòng Base/Smart/Opp + một dòng gợi ý; `#dcaHeroMeta` một dòng `nowrap`. Bảng dùng `.tbl.compact` (chữ 12px, `nowrap`, **`tbody tr{height:31px}`** vì chip ngân sách `Base 5.000k/th` cao hơn chữ thường 3px). Kết quả đo: 4 card giữ nguyên pixel ở cả 4 coin tại 1920 (468/468/244/244) và 1440 (492/492/244/244). **Bản trước pin `min-height` đo ở 1440 — sai ở 1920 vì nhãn không còn xuống dòng, tạo khoảng trống lớn; đừng quay lại cách đó.**
  - **Ô thống kê `.stats.three.tight`:** nhãn một dòng, **số và chú thích cùng một hàng** (grid `auto 1fr`, baseline), chú thích và nhãn `nowrap + ellipsis`, `min-height:58px` ⇒ ô cao bằng nhau ở mọi coin. Thành phần họ coin (`1,388 ETH + 0,591 WBETH`, 3 số lẻ — 4 số lẻ thiếu đúng 2px) nằm trên **nhãn** ô Đang giữ, hàng số chỉ còn "vốn ròng" nên không bị cắt. Bỏ ô "Giá hiện tại" (hero đã có).
  - **Ví Coin mỗi coin đúng một hàng:** `table-layout:fixed` + `colgroup` 22/18/22/19/12/7% — Tên · Số lượng (ô nhập) · Tỷ giá (USDT) · Giá trị (USDT) · Giá trị nghìn đồng · xoá. Tỉ giá 1 đơn vị và tổng USDT tách thành **hai cột** thay vì hai tầng trong một ô (`coinPriceCell()` đã bỏ). Bỏ hai nút ↑↓ (chỉ giữ xoá), tên coin ellipsis.
  - Nút Lịch sử hiện số lần mua ngay trên nhãn (`Lịch sử (23)`) để biết có gì bên trong trước khi bấm.
  - **Cả tab nằm gọn một màn hình, không cuộn** (owner yêu cầu 2026-09-13): đo `scrollHeight <= innerHeight` tại 1920×860 và 1440×900 ở cả 4 coin. `main` desktop hạ đệm đáy 60→26px (đệm 60 là dư từ thời tabbar; mobile vẫn 90px). Màn thấp hơn (1366×768) vẫn phải cuộn — chấp nhận, không nén thêm.

- **Rà soát trùng lặp toàn app, mọi tab về một màn hình (owner yêu cầu 2026-09-13).** Quy ước chung lấy từ tab DCA: card cùng hàng cao bằng nhau, không pin pixel, bảng dài → nút popup, đo `scrollHeight <= innerHeight` ở 1920×860 và 1440×900. Ba PR:
  - **Tab Lịch sử gộp vào Tổng quan** (owner chốt). Bố cục Tổng quan: hàng 1 = hero Tài sản ròng + 4 ô tight (Thu/Chi/Tích lũy/Mục tiêu) | Mốc & Dài hạn; hàng 2 `.grid3.ov` = biểu đồ thu chi + tài sản ròng 24 tháng (chính là biểu đồ tab Lịch sử cũ, thay cho sparkline "12 tháng" trùng dữ liệu) | Phân rã tăng trưởng | Cơ cấu & thanh khoản. Ba nút popup ở góc trên: **Bảng tháng (N)** (bảng chốt tháng, sửa ghi chú/xoá ngay trong popup), **Chi theo nhóm** (tháng này so TB 6 tháng + bảng nhóm × tháng), **Thu theo nguồn**. `VIEWS` còn 5, `VIEW_ALIAS.lichsu → tong`, tabbar `repeat(5,1fr)`, `testViews` có thêm ca alias này.
  - **Popup từ DOM có sẵn — `openHolder(id, title)`:** ba bảng dài nằm sẵn trong `#ovHolders` (`.holders{display:none}`), lúc mở thì **chuyển node vào modal**, đóng thì trả về chỗ cũ (`showModal` nhận thêm `opts.onClose`; phím Esc giờ đi qua đúng `done()` chứ không chỉ `hidden=true` — nếu không node kẹt trong modal và bị `innerHTML=''` xoá ở lần mở sau). Nhờ vậy listener gắn theo id (`histTable` change/click, `ovUnusual`) chạy y nguyên trong popup, không viết lại.
  - **Cài đặt hai cột** (`.settings` grid 2 cột, `.scol`): cột trái mục tiêu/mốc/dài hạn/nhóm chi/nhóm thu, cột phải DCA/tài khoản/nâng cao — vừa một màn 1920×860 (trước là một cột 640px, cuộn thêm 427px).
  - **Dòng tiền cuộn trong card** (owner chốt, thay vì popup vì đây là chỗ nhập liệu hằng ngày): trên desktop `#view-dongtien.active` là flex cột cao `calc(100vh − 52px)`, hàng thống kê giữ nguyên, `.ledger4` nhận phần còn lại, mỗi `.col` là flex cột và `.scroll` bên trong `overflow-y:auto` — form Thêm luôn nằm trên, chỉ danh sách cuộn. Đo 1920×860: trang không cuộn, 4 card 549px, danh sách Chi 272px thấy ~7 dòng rồi cuộn trong card. Điện thoại giữ dòng chảy thường (media ≥821px).
  - **Tài sản** (owner chốt: "ghi chung là Binance, hiển thị tổng quy USDT; tài sản khác thả xuống, bấm mới hiện chi tiết"): Danh mục là bảng **theo nhóm** — mỗi nhóm (Tiền/Vàng/Bạc/Chứng khoán/Khác) một dòng tổng `tr.grp[data-g]`, bấm mở/gập (`state.holdOpen`), bên trong mới là từng tài sản sửa tay; coin gộp **một dòng "Binance"** = tổng quy USDT + nghìn đồng, link DCA, không thả xuống. ↑↓ chỉ đổi chỗ trong cùng nhóm. **Bảng giá chỉ còn khoá nhập tay** (usdt, vàng, bạc, hoa khai) đặt cạnh Danh mục; giá coin live xem ở Ví Coin DCA, nút "Cập nhật giá live" bỏ (trùng nút Cập nhật của DCA). Donut Cơ cấu bỏ khỏi tab này (đã có ở Tổng quan kèm thanh khoản). Ba popup qua `openHolder`: **Chuyển đổi** (form `formXfer` nguyên vẹn, listener giữ nguyên), **Lịch sử chuyển đổi (N)** (hiện đủ, không cắt 20), **Kịch bản**.
  - **Kịch bản thông minh** (owner yêu cầu: thanh trượt + biểu đồ đổi theo khi trượt): đầu popup có bộ chọn tài sản (`Crypto` = mọi coin cùng một %, hoặc từng khoá giá đang giữ), thanh trượt −80…+300 %, ô gõ giá, preset % nhanh và **preset từ Vùng giá mua của DCA** (`D.prices.dca.coins[k].bands`). `scnApplyPct(k, pct)` ghi vào đúng `state.scn.prices` cũ nên bảng giá giả định, "giá cần để đạt mốc", lưu/xoá kịch bản đều dùng lại nguyên. Biểu đồ `scnChartSvg`: chiếu tài sản ròng 24 tháng = NW theo kịch bản + tích lũy TB 6 tháng × t (đường đậm) so với giá hiện tại (đường mờ), vạch mốc kế tiếp/dài hạn, ghi chú "chạm mốc sau N tháng (T…)" — `scnProjection()` hàm thuần, `testScenario()` 12 ca. `.dialog` có `max-height:calc(100vh − 40px); overflow:auto` vì popup này cao hơn màn 860.
  - Ô "Mục tiêu" chữ dài xếp hai dòng (`.stats.tight .stat.wide`); Cơ cấu ở Tổng quan dùng donut 110px + chú giải ellipsis vì cột chỉ 310px ở 1440.

- **Phân rã tăng trưởng — phần "Thị trường" lấy giá ở đâu (owner hỏi 2026-09-13, đã rà lại toàn bộ đường đi):**
  - Chuỗi thật: `refreshDca()` (nút Cập nhật ở DCA) ghi giá Binance vào `D.prices[coin].value = giá USD × tỷ giá usdt nhập tay` → `currentSnapshot()` gọi `holdingSnapshot()` dùng đúng `price(h.priceKey)` → `attribution()` lấy hiệu số giá trị giữa hai snapshot. **Vậy phần Thị trường ăn đúng giá coin live**, nhưng chỉ mới tới mức "lần bấm Cập nhật gần nhất", và phần quy ra nghìn đồng vẫn phụ thuộc tỷ giá `usdt` nhập tay. Vàng/bạc/chứng khoán chỉ đổi khi owner tự sửa giá.
  - **Điều kiện bắt buộc:** `marketKnown` chỉ bật khi **cả hai** snapshot có mảng `holdings`. 19 tháng chốt từ file Excel **không có** `holdings`/`prices` (đo thật: `cóHoldings: []`) ⇒ hiện tại app không tách được phần giá, tất cả rơi vào "Khác". Chỉ cần bấm **Chốt tháng** một lần trong app là từ đó có số. Hai chỗ ghi chú đã nói thẳng điều này thay vì câu chung chung "cần chốt tháng có snapshot".
  - **Lỗi đã sửa:** công nợ tính bằng **chỉ vàng** đổi giá trị khi giá vàng đổi, nhưng `attribution()` chỉ quét `holdings` ⇒ phần đó rơi vào "Khác" và bật cờ cảnh báo sai (đo: vàng +1.000k/chỉ ⇒ Thị trường +3.000k đúng phần danh mục, còn +3.000k của công nợ vàng nằm ở Khác). Thêm `debtQtySnapshot()` (số chỉ ròng theo khoá giá, họ nợ mình − mình nợ) vào `closeMonth`/`currentSnapshot`/`snapshotOf`, và `attribution()` cộng `qty₀ × (giá₁ − giá₀)` — **chỉ hiệu ứng giá trên số lượng đầu kỳ**; vay thêm hay tất toán giữa kỳ là sự kiện thật nên vẫn thuộc "Khác". Bản chốt cũ không có `debtQty` thì giữ nguyên cách cũ, không lỗi. `testAttribution` thêm ca 8 (25 → 32 ca).

- **Không dựng lại quá khứ, chỉ chạy từ giờ (owner chốt 2026-09-13).** Đã hỏi: file cũ không có bảng danh mục cuối từng tháng, nên 19 tháng import (2025-01 → 2026-07) giữ nguyên là số thu/chi/tài sản ròng, không có ảnh chụp. Owner cũng xác nhận **trước 10/2025 chưa có coin** (dòng Chuyển đổi sớm nhất 08/10/2025) — ghi lại phòng khi sau này muốn dựng lại: số lượng coin từng tháng suy ngược được từ 68 dòng Chuyển đổi (tất cả đều có `usdtRate`), giá coin lấy Binance klines lịch sử; vàng/bạc thì không có nguồn nào, phải owner cung cấp.
- **Bản chốt lưu NGÀY CHỤP THẬT `snapAsOf` (sửa 2026-09-13).** Hầu như không ai bấm Chốt tháng đúng 23:59 ngày cuối tháng: chốt T8 vào 13/09 thì tài sản ròng của mốc **đã gồm** thu chi 01→13/09, nhưng `attribution()` lọc theo tháng nên đếm những khoản đó thêm lần nữa vào Dòng tiền, phần lệch rơi vào "Khác". Giờ `closeMonth` ghi `snapAsOf: todayStr()`, `snapshotOf`/`currentSnapshot` trả kèm, và `inPeriod` **lọc theo ngày chụp khi có** (bản chốt cũ + tháng import không có `snapAsOf` ⇒ giữ nguyên lọc theo tháng, không đổi hành vi). Đo thật: thu 5.000k trước khi chốt muộn ⇒ trước là Dòng tiền +5.000k / Khác −5.000k, sau là 0/0. Nhãn đầu tab ghi rõ "Mốc so sánh: bản chốt T8/2026 chụp 13/09". `testAttribution` thêm ca 9 (32 → 35 ca).

- **Tự lấy giá và tự chụp mỗi lần mở app (owner yêu cầu 2026-09-13).**
  - `openTasks()` chạy sau `route()` trong `start()`: gọi `refreshDca({silent:true})` nếu cache `prices.dca` cũ hơn `OPEN_REFRESH_MIN` = 10 phút (reload liên tục không gọi liên tục), rồi `takeDailySnapshot()`. Chế độ `silent` **không toast và không đổ stack đỏ ra console** — mất mạng một lúc là chuyện thường, số cũ vẫn dùng được; nút Cập nhật bấm tay vẫn báo lỗi như cũ.
  - **Collection mới `snaps`** (id = `YYYY-MM-DD`) lưu đúng thứ bản chốt tháng lưu: `holdings` + `prices` + `debtQty` + `netWorth`. Ghi đè trong ngày, giữ `SNAP_KEEP_DAYS` = 120 ngày rồi tự xoá (`snapsToPrune`). Không ghi khi danh mục rỗng (tránh mốc rác lúc chưa import). Đã thêm vào `COLLECTIONS`, `loadAll` của **cả hai** adapter, `backupPayload` và `restoreBackup`.
  - **Kỳ theo ngày trong Phân rã tăng trưởng:** `PERIODS` thêm `d7`, `d30`. `periodStats` rẽ nhánh: mốc đầu = ảnh chụp gần nhất không sau (hôm nay − N ngày) qua `snapAtFrom`; **chưa có ảnh nào đủ cũ thì lấy ảnh cũ nhất đang có** và `rangeLabel` ghi khoảng thật (`10/09 → hôm nay`) — không bắt owner chờ đủ N ngày mới có số. Ảnh chụp ngày mang `snapAsOf` nên tự hưởng luôn cách lọc theo ngày đã làm trước đó. `testSnaps()` 14 ca.
  - **Chốt tháng nhiều lần là an toàn:** `closeMonth` dùng `store.set` nên ghi đè đúng một document (đo: chốt T9 hai lần ⇒ vẫn 20 bản chốt). Hộp thoại Chốt tháng giờ nói thẳng điều đó, và nhắc nếu chốt khi tháng chưa kết thúc thì nên dùng kỳ 7/30 ngày để xem tình hình thay vì chốt sớm.

- **Card "Chi phí vốn" ở tab Tài sản (owner chốt 2026-09-17, sau buổi rà "tăng thu nhập").** Bối cảnh owner xác nhận: làm phân phối hàng điện tử, thu nhập = lương + hoa hồng nhiều nguồn. Dữ liệu chỉ ra "thu nhập" chắc nhất là chặn rò rỉ lãi: thẻ VP 25 tr trong khi USDT ~14,5 tr + tiền mặt nằm không.
  - Khoản nợ (cả hai chiều) có thêm trường tuỳ chọn **`rate` (%/năm)** — ô "Lãi %/năm (nếu có)" trong form Dòng tiền, hiện nhỏ dưới tên khoản (`36%/năm`). Không ghi thì app **không đoán**, chỉ đếm "N khoản chưa ghi lãi" và nhắc điền.
  - `capitalCost(debts, holdings, {debtValue, holdingValue})` hàm thuần: lãi tháng = giá trị × rate/100/12 (lãi đơn, đủ để so sánh), tách trả/thu, bỏ khoản đã tất toán; **tài sản nằm không** = nhóm Tiền không khoá giá + USDT (chứng khoán, vàng không tính). Gợi ý duy nhất: trả khoản lãi **cao nhất** bằng tài sản nằm không (USDT trước, tiền sau) ⇒ đỡ bao nhiêu/tháng — phép tính, không phải lệnh. `testCapital()` 12 ca.
  - Card đặt dưới hàng Danh mục | Bảng giá (tab Tài sản vốn còn trống nửa màn), 3 ô tight + dòng gợi ý + bảng compact các khoản có lãi. Tài sản vẫn không cuộn ở 1920×860 và 1440×900.

- **Tab Trading — swing H4, backtest + paper (mở 2026-09-17, owner chốt "backtest + paper trước, khung giữ lệnh 3–14 ngày").** ⚠️ **UI của phần H4 (hero điểm swing, lưới 9 bộ ba chế độ, Tín hiệu) đã GỠ 2026-09-17 theo yêu cầu owner** — tab Trading giờ là bảng theo dõi 100 coin + 100 cổ phiếu (xem PR 7 bên dưới). Các hàm thuần (`SWING_SCORE_V1`, `simulateLadder`, `tradePlan`, `TRADE_MODES`, `holdReturn`, `tradeBench`…) và `testSwing` 70 ca **giữ nguyên** vì backtest cổ phiếu/coin dùng chung; `refreshTrade`, `renderTrade` cũ, cache `prices/current.trade` không còn được ghi. Ba mục v1/v2/v3 dưới đây giữ làm nhật ký kết quả backtest.
  - **Cùng thuật toán, khác cửa sổ.** `buyScore(eth, btc, opts)` nhận thêm `opts.config` (mặc định `BUY_SCORE_V1`, không đổi hành vi DCA — ngưỡng LOW `n<250` giờ viết là `minCandles×1,25` cho ra đúng 250). `SWING_SCORE_V1`: nến `4h`, `klineLimit` 1.000 (≈ 167 ngày), `minCandles` 200, `staleHours` 8, cửa sổ `{year:180, month:42, volShort:6, volLong:180, rsi:14, ma:50, ethbtc:42}` = 30 ngày · 7 ngày · 24 giờ · 30 ngày · RSI14 · MA50 · 7 ngày; các bậc điểm thu hẹp tương ứng (giảm từ đỉnh 30 ngày tối đa 30%, lệch MA50 ±12%…). **Ngưỡng này là điểm xuất phát, không phải kết luận** — backtest đo thật. Cùng 7 khoá thành phần nên `scoreTableHtml(comps, score, note, config)` dùng chung; nhãn đo (`MA50 2250`, `ETH/BTC 7 ngày`) lấy từ config.
  - **Ba hàm thuần, `testSwing()` 21 ca:** `swingScoreSeries(candles, ref, cfg)` chấm điểm tại TỪNG nến chỉ với dữ liệu tới nến đó (không nhìn trước; 2×800 lần chấm ≈ 200 ms trong trình duyệt). `simulateSwing(series, rules)`: mua/bán tại giá đóng nến, phí 0,1% hai chiều, tối đa một lệnh, toàn bộ vốn; ra khi lãi ≥ `tp`, lỗ ≥ `sl`, điểm ≤ `exit` (25), giữ ≥ `maxHoldBars` (84 nến = 14 ngày) hay hết dữ liệu; **không mở lệnh ở nến cuối và không mở lại ngay nến vừa đóng**; trả `netReturn`, `winRate`, `maxDD` (mark-to-market từng nến), `buyHold` cùng kỳ, `exposure`. `backtestGrid(series)`: lưới `SWING_GRID` = entry {45,55,65} × (tp,sl) {6/4, 10/5, 15/7} = 9 bộ, xếp theo lãi ròng.
  - **`refreshTrade()`** chỉ chạy khi bấm nút (hai lệnh 1.000 nến nặng hơn DCA, không đưa vào `openTasks`). Cache `prices/current.trade = {coins:{eth,btc:{score, components, grid, buyHold, bars, from, to…}}}` — không lưu nến, không lưu chuỗi điểm.
  - **Tab `#trade`** (VIEWS 6, tabbar `repeat(6,1fr)`): hàng 1 = hero điểm swing + phân rã (luôn 7 dòng) | bảng backtest **luôn 9 dòng** theo lưới (điền "—" khi chưa chạy, dòng tốt nhất `tr.best` chỉ khi lãi > 0); hàng 2 = "Tín hiệu hôm nay" so điểm hiện tại với ngưỡng vào của bộ tốt nhất, ghi rõ giá chốt lời/cắt lỗ/hạn nếu vào. **Nói thẳng khi thua mua-giữ:** "Không bộ nào thắng mua-giữ ⇒ chưa đáng trade, tiếp tục DCA" — tab này sinh ra để loại bộ tệ, không để tin bộ tốt. Đo: 2 card hàng 1 bằng nhau ở ETH/BTC (485/485), không cuộn ở 1920×860 và 1440×900.
  - **Chưa có** sổ lệnh giả (PR kế: collection `trades`, ghi lệnh giả theo tín hiệu, chấm PnL theo giá thật; chỉ bàn tiền thật khi backtest và paper cùng dương). Kỳ vọng đã nói với owner: sleeve ~20 tr, 3%/tháng là ~600k — giá trị là kỷ luật có số liệu, không phải thu nhập.

- **Chi phí vốn tạm ẩn + kế hoạch lệnh có số (owner 2026-09-17).**
  - Owner cho biết nợ thẻ VP được **đảo liên tục** bằng tiền mua hàng công ty nên gần như không mất lãi ⇒ card Chi phí vốn **ẩn** bằng cờ `CAPITAL_CARD_ENABLED = false` (card `#capCard` và ô "Lãi %/năm" trong form nợ cùng ẩn; code, `capitalCost`, `testCapital`, trường `rate` giữ nguyên). Mở lại = đổi cờ thành `true`.
  - **Cài đặt → "Trading — vốn dành cho trade"** (`settings.trade.sleeve`, nghìn đồng, `normalizeSettings` mặc định 0). 0 ⇒ card Tín hiệu chỉ hiện % và quy tắc, không ra số tiền.
  - **`tradePlan(score, priceUsd, rules, sleeveK, usdtRate)`** hàm thuần (8 ca trong `testSwing`, 21 → 29): khối lượng = **rủi ro% / cắt lỗ%** (rủi ro = % vốn trade mất nếu chạm cắt lỗ), rủi ro theo bậc `SWING_SIZING_V1.riskTiers` = điểm vượt ngưỡng vào ≥ 0/10/20 ⇒ 1% / 1,5% / 2%, kẹp một lệnh ≤ 40% vốn trade, tối đa 2 lệnh mở, vốn trade sụt 10% trong tháng ⇒ dừng. Trả giá cắt lỗ, chốt lời, số USD, số coin, số tiền mất tối đa.
  - Card "Tín hiệu hôm nay" giờ in đủ: VÀO/CHỜ, khối lượng nghìn đồng ≈ USD = coin, mất tối đa bao nhiêu, giá vào, **đặt ngay OCO trên Binance** (chốt lời + cắt lỗ), hạn thoát theo ngày, và dòng cảnh báo khi bộ tốt nhất chưa thắng mua-giữ. Quy tắc thoát vẫn đúng bộ đã backtest — không thêm trailing stop hay dời stop về hoà vốn vì chưa backtest.

- **Trading v2 — thoát theo bậc, vào 2 nhịp, lịch sử 2,5 năm (owner chốt 2026-09-17: "cắt lỗ dần thay vì một lần, chốt lời dần thay vì một lần").**
  - **Vì sao đổi:** backtest thật v1 thua mua-giữ ở cả 9 bộ, cả hai coin. Hai lỗi của v1: (1) ra lệnh khi **điểm ≤ 25** — điểm đo độ *rẻ*, giá vừa hồi là điểm tụt, bán đúng lúc sóng mới bắt đầu; (2) 5,5 tháng dữ liệu (1.000 nến) là **một trạng thái thị trường**, không tách được "cách chơi sai" với "thị trường không hợp".
  - **`SWING_RULES_V2` / `SWING_LADDERS`:** bỏ hẳn ra-theo-điểm. Vào 2 nhịp (`addOn`: 50% ngay, 50% còn lại khi giá rớt thêm 3% mà điểm vẫn ≥ ngưỡng; giá vốn = bình quân theo **tiền** nên hai nhịp bằng tiền ra trung bình điều hoà, 100 & 97 ⇒ 98,48 chứ không phải 98,5). Chốt lời 2 bậc, cắt lỗ 2 bậc (bậc đầu bán 50%, bậc sau bán hết), **`beAfterTp`**: đã ăn bậc chốt lời đầu thì phần còn lại về ≤ giá vốn là bán (dời cắt lỗ về hoà vốn). Vẫn giữ hạn 84 nến (14 ngày). Ba thang L1 (5/10 · −3/−6), L2 (8/15 · −4/−8), L3 (10/20 · −5/−10) × ngưỡng vào {55,65,75} = 9 bộ (`SWING_GRID_V2`).
  - **`simulateLadder(series, rules)`** hàm thuần riêng, `simulateSwing` v1 giữ nguyên làm **đối chứng** (dòng "Cách cũ tốt nhất" dưới bảng). So sánh mốc TP/SL có **epsilon 1e-9**: `92/100−1 = −0,0799…` không ≤ −0,08 trong số thực — không có epsilon thì bậc cắt lỗ thứ hai đúng ngay mức không bao giờ kích.
  - **Lịch sử 2,5 năm:** `fetchKlinesDeep()` gọi klines nhiều trang lùi bằng `endTime` (`klineTotal` 5.400 nến = 6 lệnh/coin, vẫn endpoint công khai). `swingScoreSeries` chỉ nhìn `seriesWindow` 400 nến gần nhất khi chấm mỗi nến ⇒ 2×5.400 lần chấm ≈ 0,8 s trong trình duyệt. Cache đổi `version:'swing-h4-v2'`; `tradeCache()` bỏ cache khác version nên bản v1 cũ hiện "chưa chạy" thay vì vẽ sai cột.
  - **`backtestGrid2()` đo hai kỳ:** toàn kỳ và **12 tháng gần** (nến có `time ≥ cuối − 365 ngày`, biên bao gồm nên 2.191 nến chứ không 2.190), kèm mua-giữ mỗi kỳ. Bảng 8 cột: Vào ≥ · Chốt lời dần · Cắt lỗ dần · Lệnh · Thắng · Toàn kỳ · 12 th gần · Sụt tối đa. Kết luận 4 trường hợp: thắng cả hai ⇒ lợi thế ổn định; thắng toàn kỳ thua 12 th ⇒ phụ thuộc trạng thái, chờ; thắng 12 th thua toàn kỳ ⇒ có thể may; thua cả hai ⇒ tiếp tục DCA. Chỉ khi thắng **cả hai** mới bỏ dòng cảnh báo "chỉ ghi lệnh giả".
  - **`tradePlan()` v2:** lỗ tối đa trên khối lượng = Σ (phần bán ở bậc × |mức|) (−4% 50% rồi −8% hết ⇒ 6%), khối lượng = rủi ro% / lỗ tối đa; trả `lots` (2 nhịp, giá nhịp 2), `tpLevels`, `slLevels`. `maxLossPct` trả cả khi chưa tới ngưỡng vào để dòng quy tắc không in "0%". Card Tín hiệu hướng dẫn **2 lệnh OCO** (A: TP1/SL1, B: TP2/SL2, mỗi lệnh 50% coin) và sửa SL của B lên giá vốn khi A khớp chốt lời.
  - Đo: hai card hàng 1 bằng nhau ở ETH/BTC (831×485 tại 1920, 591×485 tại 1440), không cuộn ở 1920×860 và 1440×900; điện thoại bảng cuộn ngang trong card. `testSwing` 29 → 47 ca.

- **Trading v3 — Futures long/short 1x làm đối chứng (owner hỏi 2026-09-17 "thị trường giảm thì dùng hợp đồng tương lai?", chốt: backtest 1x có funding trước, không tiền thật).**
  - **Điểm short không có thuật toán mới:** `sellScore()` = chính `buyScore()` chấm trên **nến đảo** (`invertCandles`: close → 1/close, khối lượng và thời gian giữ nguyên). Giảm-từ-đỉnh thành tăng-từ-đáy, RSI thành 100−RSI, percentile lật ngược, lợi suất 7 ngày đổi dấu, ETH/BTC thành BTC/ETH — cùng ngưỡng, cùng trọng số. `swingScoreSeries` trả thêm `sell` mỗi nến (2× thời gian chấm, ≈ 1,7 s cho 2×5.400 nến). Hero hiện "Điểm short N/100" thay chỗ "Buy Score DCA" (xem ở tab DCA).
  - **`TRADE_MODES` ba chế độ cùng lưới 9 bộ, cùng 8 cột:** `spot` (chỉ long, phí 0,1%), `ls` (long khi điểm mua ≥ ngưỡng, short khi điểm short ≥ ngưỡng, 1x), `short` (chỉ short, 1x). Bộ chọn `.seg` ở đầu card Backtest (`state.tradeMode`), nhãn ngắn `Spot · L/S 1x · Short 1x` vì nhãn dài làm nút xuống dòng. Mô tả chế độ nằm ở dòng ghi chú dưới bảng, không ở tiêu đề.
  - **`simulateLadder` nhận `side` + `fundingPerBar`:** short mô phỏng ở 1x — ký quỹ = giá trị vị thế lúc vào, giá trị đóng = `units × (2·avg − P)`; pnl = `dir × (P/avg − 1)` nên bậc TP/SL/hoà vốn dùng chung; nhịp 2 của short khi giá **lên** thêm 3%. `FUTURES_V1`: phí taker 0,05%, **funding 0,01%/8h giả định trả ở CẢ HAI chiều** (bất lợi có chủ ý — không tải lịch sử funding; thực tế bên này trả thì bên kia nhận). Không mô phỏng đòn bẩy: nếu 1x không thắng cả hai kỳ thì đòn bẩy chỉ nhân lỗ. Chế độ mặc định `long`, `fundingPerBar` 0 ⇒ spot chạy y như v2.
  - `backtestGrid2(series, grid, mode)`; đối chứng cách cũ (`v1Best`) chỉ tính cho spot. Cache `prices/current.trade.coins[k].modes = {spot, ls, short:{rows, v1Best, v1BestY1}}` + `sell`; `grid` giữ = spot; version `swing-h4-v3` nên cache v2 tự coi là chưa chạy.
  - `tradePlan(..., rules.side)`: short lật mốc — chốt lời ở **dưới**, cắt lỗ ở **trên**, nhịp 2 ở trên; khối lượng theo rủi ro như long. Card Tín hiệu theo chế độ đang xem: `VÀO LONG` / `VÀO SHORT` / `CHỜ` (ghi cả hai khoảng thiếu); hướng dẫn Futures = 1x, isolated, 2 cặp TP/SL 50%. Kết luận vẫn so với `max(0, mua-giữ)` ở mọi chế độ — short phải thắng cả tiền mặt và cả việc cứ ôm coin.
  - Mốc thời gian dùng `fmtDayY` (dd/mm/yy) vì kỳ dài hơn một năm — bản trước in "12 th gần từ 17-09" nhìn như hôm nay.
  - Đo: 2 card hàng 1 bằng nhau ở 2 coin × 3 chế độ (831×485 tại 1920, 591×485 tại 1440), không cuộn ở 1920×860 và 1440×900. `testSwing` 47 → 66 ca.
  - **Mốc so sánh phải theo chiều của chế độ (sửa ngay sau backtest thật 2026-09-17).** Bản đầu so mọi chế độ với mua-giữ ⇒ ETH Short 1x (+5,2% · +15,4%) được khen "thắng ở cả hai kỳ" trong khi chỉ cần **short rồi ôm** đã ăn cả cú rớt 20%/46%. `holdReturn(series, side, mode)` = ôm một chiều từ nến đầu tới cuối với đúng phí + funding (chạy qua chính `simulateLadder`, TP/SL vô cực), `tradeBench(mode, M, c)` chọn mốc: spot ⇒ mua-giữ, chỉ short ⇒ **short-giữ 1x**, L/S ⇒ **chiều ôm tốt hơn** (biết trước — cố ý khó). Kết luận thêm câu "sụt tối đa lớn hơn lãi toàn kỳ ⇒ chưa đáng tiền thật" khi đúng thế. Version `swing-h4-v4` để cache v3 tính lại. `testSwing` 66 → 70.

- **Vỏ iOS bằng Capacitor (owner yêu cầu 2026-09-17, đã có Mac; chọn mức 2 thay vì PWA).** Thư mục `ios-app/`, hướng dẫn từng bước cho owner ở `IOS.md`.
  - **Vỏ không chứa mã web**: `capacitor.config.json` đặt `server.url` = GitHub Pages ⇒ merge lên `main` là app trên điện thoại có bản mới, không build lại. `www/` chỉ có `index.html` (chuyển hướng) và `error.html` (`server.errorPath`, trang mất mạng). Nguyên tắc "một file, không build step" cho web **giữ nguyên**; build step duy nhất là Xcode trên Mac của owner, và chỉ khi cài lại.
  - Capacitor 8 + **SPM** (`capacitor-swift-pm` từ GitHub) ⇒ Mac không cần Node/CocoaPods; owner chỉ Download ZIP → mở `ios-app/ios/App/App.xcodeproj` → chọn Team → ▶ Run. `ios/.gitignore` sửa để **giữ** `App/App/public` + `App/App/capacitor.config.json` trong git vì owner không chạy `cap sync`. `node_modules` gitignore ở root.
  - `index.html`: viewport `viewport-fit=cover`, meta `apple-mobile-web-app-*`, `theme-color`; `main` mobile chừa `env(safe-area-inset-top/bottom)` (tabbar đã có từ trước). Không đổi gì khác trong app.
  - Icon 1024 và splash 2732 sinh bằng Playwright screenshot (`icon.mjs` trong scratchpad; vòng tròn + 3 cột, nền ink). Đăng nhập email/mật khẩu chạy được trong WKWebView; **không** đổi sang popup Google.
  - Chưa làm (chờ owner cần rõ): Face ID, widget, thông báo, TestFlight/App Store (cần Developer Program 99 USD/năm; Apple ID miễn phí = 7 ngày phải Run lại).

- **Tab Chứng khoán — PR 1: dò nguồn dữ liệu + khung VN30 (owner yêu cầu 2026-09-17: "áp toàn bộ phân tích coin cho chứng khoán VN, top 30, rồi swing khung 4H").** Đã trả lời trước khi làm: thuật toán chuyển được nguyên xi (Buy Score nhận mảng nến bất kỳ; thành phần 7 đổi thành **so với VN-Index**), nhưng ba chỗ khác hẳn: (1) **không có 4H** — sàn mở 4,5 giờ/ngày nên khung swing là **nến ngày** cửa sổ rút ngắn; (2) luật T+2, biên ±7%, không short cổ phiếu lẻ, phí + thuế bán ≈ 0,4–0,6%/vòng ⇒ mô phỏng PR 3 phải ghi đúng; (3) **không có API chính thức**, nguồn công khai không chính thức có thể chặn CORS — sandbox không ra mạng nên phải dò trên Pages thật. Owner chốt: PR 1 dò nguồn trước; DCA chứng khoán sau này nhắm **từng cổ phiếu theo điểm** (không phải ETF).
  - Tab `#chungkhoan` (VIEWS 7, tabbar `repeat(7,1fr)`, nhãn mobile "Cổ phiếu" cho vừa 55px). Hai card dọc: **Nguồn dữ liệu** (3 dòng TCBS · SSI iBoard · VNDirect: trạng thái, số nến FPT, nến cuối, VN-Index, ms) và **VN30** (luôn đủ 30 dòng: Giá · Hôm nay · Phiên · Ghi chú lỗi). Desktop: `#view-chungkhoan.active` flex cột cao `calc(100vh − 52px)` như Dòng tiền, bảng VN30 cuộn **trong card** — đo 1920×860: trang không cuộn, bảng 962/561 cuộn trong.
  - `STOCK_SOURCES[]`: mỗi nguồn `{key, label, url(sym, kind, fromMs, toMs, n), parse(json), noIndex?}` — TCBS `stock-insight/v2/stock/bars-long-term` (có `type=index`), SSI `iboard.ssi.com.vn/dchart/api/history` (UDF `{t,o,h,l,c,v}`), VNDirect `finfo-api/v4/stock_prices` (không có chỉ số). **Endpoint chép theo cách `vnstock` và cộng đồng dùng, chưa gọi được thật từ sandbox** — bảng dò sinh ra để biết nguồn nào sống.
  - `normalizeStockCandles(rows, kind)` hàm thuần: bỏ dòng hỏng, xếp tăng, gộp trùng ngày, time về 0h UTC; **tự nhận đơn vị**: cổ phiếu có trung vị close > 1000 là VND ⇒ chia 1000 về nghìn đồng; `kind:'index'` giữ nguyên điểm (VN-Index ~1.300 cũng > 1000). `probeErrorLabel(e)`: TypeError ⇒ "trình duyệt chặn (CORS) hoặc mất mạng", AbortError ⇒ quá 12 giây, còn lại giữ thông điệp HTTP.
  - `refreshStocks()`: `probeStockSources()` lần lượt 3 nguồn (FPT 600 nến rồi VN-Index), nguồn đầu tiên sống ⇒ `loadVn30Quotes()` 30 mã, 4 luồng song song, 6 nến/mã. Lưu `prices/current.stocks = {probe, probedAt, source, quotes, quotesAt, errors}`. Không đưa vào `openTasks` (chưa biết nguồn nào chịu được 30 lệnh mỗi lần mở).
  - Rổ `VN30` là hằng số (`VN30_BASKET` ghi kỳ) — HOSE đổi rổ mỗi 6 tháng, sửa tay. `testStocks()` 17 ca (parse 3 định dạng, chuẩn hoá đơn vị, nhãn lỗi). Ảnh chụp bảng dò của owner quyết định PR 2 (Buy Score nến ngày cho 30 mã, giá tự vào nhóm Chứng khoán ở Tài sản) và PR 1b (Cloudflare Worker proxy) nếu cả ba bị chặn.

- **Chứng khoán PR 1b — cả 3 nguồn bị chặn CORS từ điện thoại (owner gửi ảnh 2026-09-17) ⇒ crawler GitHub Actions, kho cùng origin.**
  - **Kết quả dò thật:** TCBS và SSI trả "trình duyệt chặn (CORS)", VNDirect quá 12 giây. Sandbox cũng không gọi được (proxy chặn host) nên **không kiểm chứng được endpoint ở đây** — chỉ máy chạy Actions mới biết. Owner chốt "thử hướng khác hoặc crawler".
  - **Cách làm:** `scripts/crawl-stocks.mjs` (Node 20, không gói ngoài) cào nến ngày 30 mã + VN-Index từ **5 nguồn theo thứ tự** TCBS → VCI/Vietcap (POST `gap-chart`) → SSI → VNDirect → CafeF, mã nào lấy được thì ghi nguồn, thiếu thì ghi lý do vào `errors`; **gộp với kho cũ** (không bao giờ mất nến đã có, giữ 700 nến), quy nghìn đồng cùng quy tắc trung vị > 1000 như app. Ghi `data/vn30.json` dạng nén `rows:[[t,o,h,l,c,v]]` (~700 KB). Thoát mã 1 chỉ khi **không mã nào** lấy được để run đỏ rõ ràng.
  - `.github/workflows/stocks.yml`: cron `40 8 * * 1-5` (15:40 giờ VN, sau ATC) + `workflow_dispatch`; `permissions: contents: write`, commit "data: giá VN30 …" bằng `fintrace-bot`. Mỗi commit kích Pages deploy lại (~1 phút). **GitHub tắt lịch sau 60 ngày repo không có commit** — app hiện nhãn "cũ" khi kho quá `STOCK_STORE_STALE_H` = 30 giờ và nhắc vào Actions bấm Run workflow.
  - **App:** `refreshStocks()` đọc `data/vn30.json` (tương đối, cùng origin ⇒ không CORS) **trước**; được thì `stockStore` giữ toàn bộ nến trong RAM (không ghi Firestore — 31 × 600 nến vượt 1 MB/document), `quotesFromStore()` ra giá VN30, ba nguồn trực tiếp ghi "bỏ qua — đã có kho". Kho hỏng (404 lúc Actions chưa chạy lần nào) mới dò 3 nguồn như PR 1. Bảng Nguồn có hàng đầu "Kho trên Pages" và dòng ghi chú liệt kê máy cào lấy được từ nguồn nào bao nhiêu mã. `parseStockStore()` hàm thuần, `testStocks` 17 → 21.
  - **Kết quả lần chạy thật đầu tiên (2026-09-17 13:23 VN, run #1):** 31/31 mã lấy đủ 600 nến ngày từ **VCI/Vietcap** (`trading.vietcap.com.vn/api/chart/OHLCChart/gap-chart`, POST) — kho 916 KB, từ 19/04/2024 tới hôm nay, VN-Index 1.822 điểm, giá đã đúng nghìn đồng (FPT 73,2k · VIC 242,5k). **TCBS trả HTTP 403 từ máy Actions** (chặn IP trung tâm dữ liệu, giống Binance) ⇒ đổi thứ tự nguồn trong script: VCI trước, TCBS dự phòng. SSI/VNDirect/CafeF chưa cần tới nên chưa biết sống hay chết từ Actions. Chạy giữa phiên thì nến hôm đó là nến dở, lần chạy 15:40 ghi đè cùng ngày nên tự đúng.

- **Chứng khoán PR 2 — Buy Score nến ngày cho VN30, bỏ hẳn 3 nguồn gọi thẳng (owner chốt 2026-09-17 sau khi kho chạy: "nguồn không đọc được hãy bỏ đi, triển khai bước tiếp theo").**
  - **Chỉ còn một nguồn: kho `data/vn30.json`.** `STOCK_SOURCES`, `probeStockSources`, `loadVn30Quotes`, `fetchStockCandles`, `normalizeStockCandles` (bản app) và card "Nguồn dữ liệu" đã gỡ khỏi `index.html` — bản chuẩn hoá chỉ còn trong `scripts/crawl-stocks.mjs`. Mở tab lần đầu tự tải kho (`stockAutoTried`, im lặng); nút Cập nhật tải lại. Kho lỗi ⇒ toast chỉ đường vào Actions → Run workflow.
  - **`STOCK_SCORE_V1`**: cùng `buyScore()` 7 thành phần, cửa sổ tính bằng **phiên** `{year:250, month:21, volShort:5, volLong:60, rsi:14, ma:200, ethbtc:21}`; ngưỡng % thu còn ~70% của crypto (giảm từ đỉnh 1 năm tối đa 45%, lệch MA200 ±25%…) vì biên ±7%/phiên; thành phần 7 = **cổ phiếu so với VN-Index 21 phiên** (`ratioLabel`). `minCandles` 220, `fullCandles` 300 (kho có 600). **Điểm xuất phát, PR 3 backtest đo thật.**
  - Hàm thuần: `stockScoreOf(store, sym)` (điểm + giá + % hôm nay + drawdown 1 năm), `stockScores(store)` xếp giảm dần, `stockBands(store, sym)` dò ngược qua `priceForScore()` như coin, giá **cắt xuống bước 0,05k**. `testStocks` 21 → 18 ca (bỏ ca 3 nguồn, thêm ca chấm điểm trên kho tổng hợp: mã rớt 45% + khối lượng ×4 đứng đầu ≥ 65, mã tăng đều ≤ 25).
  - **Bố cục 2×2 như DCA:** hàng 1 = hero (giá k · Buy Score · trạng thái · độ tin cậy · phiên · xếp hạng) + Phân rã 7 dòng | **VN30 theo điểm** (30 dòng: # · Giá · Hôm nay · Điểm · Vùng · Đỉnh 1 năm, bấm dòng ⇒ `state.stockSym`); hàng 2 = Vùng giá mua (4 dòng) | Danh mục cổ phiếu. **Bảng 30 dòng cuộn trong card mà không kéo hàng grid:** `#stockListCard{height:0;min-height:100%}` (đóng góp chiều cao 0, rồi giãn bằng card hero) — `min-height:0` đơn thuần KHÔNG đủ vì track `auto` vẫn lấy max-content. Mobile reset `height:auto` + `.scroll{max-height:70vh}`. Đo: 481/481/222/222 tại 1920 và 1440, không cuộn trang, bảng 958/411 cuộn trong.
  - **Giá chảy vào Tài sản:** mỗi lần tải kho ghi `D.prices['stk:SYM'] = {value, source:'live'}` cho 30 mã ⇒ holding `{group:'Chứng khoán', priceKey:'stk:FPT', quantity: số CP}` có giá trị tự động, ô giá ở Danh mục chỉ đọc (đúng nhánh `source==='live'` sẵn có). `priceLabel('stk:FPT')` ⇒ `FPT`. Nút **Thêm cổ phiếu** (popup mã VN30 + số CP; đã có thì cộng dồn) gọi `addHolding`/`updateHolding` sẵn có; xoá trong card. Khoản "VCBS" nhập giá trị tay cũ **không đụng**. `priceSnapshot()`/kịch bản vẫn chỉ PRICE_KEYS — khoá `stk:` chưa vào Phân rã tăng trưởng phần Thị trường (holdingSnapshot có giá theo holding nên vẫn tính đúng giá trị).
  - Cache `D.prices.stocks = {storeAt, storeMs, quotesAt, errors, version, ranks:{SYM:{score,state,price,chg}}}` nhỏ; nến đầy đủ chỉ trong RAM (`stockStore`).

- **Chứng khoán PR 3 — VN100 + backtest swing nến ngày với luật VN (owner yêu cầu 2026-09-17 "thêm VN100, làm luôn bước 3").**
  - **VN100 = VN30 + `VN70`** (VNMidcap, danh sách tham khảo kỳ 2024–2025, ghi ở **hai chỗ**: `index.html` và `scripts/crawl-stocks.mjs` — sửa cùng lúc). Kho đổi tên `data/vn100.json` (file `vn30.json` cũ đã xoá), crawler giữ **800 phiên** (~3,2 năm) để chuỗi điểm sau 220 phiên khởi động còn ~2,3 năm; ~3,3 MB, Pages nén gzip. Mã không lấy được nằm trong `errors` của kho — đó là cách biết danh sách tham khảo sai ở đâu. `STOCK_SETS` + bộ chọn `.seg` **VN30 / VN100** trên card xếp hạng (`state.stockSet`, mặc định VN100); xếp hạng hero tính trên VN100.
  - **`simulateLadder` thêm 3 luật, mặc định tắt nên coin không đổi:** `minHoldBars` (T+2, tính từ lần mua **cuối** — nhịp 2 cũng phải chờ), `sellTax` (cộng vào phí bán), `floorPct` (đóng cửa sát sàn ±6,9% ⇒ không bán được; sát trần ⇒ không mua/không vào nhịp 2). Nến cuối luôn đóng để chấm điểm. `swingScoreSeries(..., {sell:false})` bỏ chấm điểm short cho cổ phiếu (không short được) ⇒ nửa thời gian.
  - **`STOCK_SWING_RULES`** = long, phí 0,15% + thuế bán 0,1% (≈ 0,4%/vòng), T+2, sàn 6,9%, giữ tối đa **15 phiên**, vào 2 nhịp, 2 bậc chốt lời/cắt lỗ; lưới `STOCK_GRID` = {55,65,75} × `SWING_LADDERS` (9 bộ, dùng chung với coin). `STOCK_SCORE_V1.seriesWindow` 300.
  - **`stockBacktestOne/All`** hàm thuần: mỗi mã 9 bộ × hai kỳ (toàn kỳ, 12 tháng = 365 ngày lịch); tổng hợp mỗi bộ = **trung bình** lãi toàn kỳ / 12 th / số lệnh / tỷ lệ thắng, cộng `posAll` (số mã có lãi), `beatBoth` (số mã bộ này thắng **cả tiền mặt và mua-giữ ở cả hai kỳ**). Kết luận: thắng mua-giữ TB cả hai kỳ **và** `beatBoth ≥ 60%` số mã ⇒ "có lợi thế trên diện rộng"; thắng TB nhưng ít mã ⇒ "tập trung ở ít mã, có thể là may"; còn lại ⇒ tiếp tục mua tích luỹ. Đo: 100 mã ≈ 3,3 s trong Chromium (38k lần chấm điểm).
  - **Popup "Backtest"** (nút cạnh Cập nhật, `showModal` wide, nút OK = "Chạy lại"): dòng luật + bảng 9 bộ tổng hợp (9 cột) + bảng 9 bộ của **mã đang chọn** + một dòng tín hiệu `VÀO/CHỜ` từ `tradePlan(score, giá k, bộ tốt nhất của mã, vốn trade, usdtRate=1)` — truyền tỷ giá 1 để `qty` ra **số cổ phiếu**, làm tròn xuống lô 100. Cache `D.prices.stocks.bt = {version, at, n, rows, bhAvg, bhAvgY1, perSym:{SYM:{bh, bhY1, rows:[[entry, bậc, lệnh, toàn kỳ, 12 th, sụt]]}}}` (~30 KB); hero meta ghi "backtest +x% · +y% bộ 65/L2" của mã đang chọn. Tab vẫn 2×2 một màn hình — bảng dài đi popup theo quy ước.
  - `testStocks` 18 → 31 ca (VN100 không trùng; T+2 chặn bán phiên 1; phiên sàn không bán, trần không mua; phí + thuế ≈ 0,4%; luật mặc định tắt không đổi coin; backtest toàn rổ trên kho tổng hợp).
  - **Kết quả thật lần đầu (kho 17/09/2026 14:00 VN, 101/101 mã từ VCI, 800 phiên, 3,95 MB / 1,3 MB gzip, backtest 3,8 s):** kỳ 21/05/24 → 17/09/26, mua-giữ TB **+16,3%** · 12 th **−13,3%**. Bộ tốt nhất 75/L2: +4,8% · +1,7%, 2,9 lệnh/mã, thắng 74%, 64/100 mã có lãi, **28/100 thắng cả hai kỳ** ⇒ kết luận "chưa đáng trade". Đọc đúng: ngưỡng 75 gần như đứng ngoài thị trường (≈ 8% thời gian có lệnh) nên năm giảm giữ được vốn (+1,7% khi thị trường −13%), nhưng cả kỳ thua xa ôm cổ phiếu. Ngưỡng 55 (9 lệnh/mã) tệ nhất: −0,5% · −0,6% — nhiều lệnh = nhiều phí + T+2. Phân bố điểm hôm đó: 2 Bán tháo · 25 Giảm sâu · 20 Tích luỹ · 30 Trung tính · 23 Đắt.

- **Chứng khoán PR 4 — nạp lịch sử lệnh VCBS, danh mục theo thực tế, bộ "Của tôi" (owner gửi 2 ảnh app VCBS 2026-09-17).**
  - **Dữ liệu từ ảnh** (bộ lọc 17/02/2025 → 17/09/2026, "Hoàn thành"): 9 lệnh mua + 1 bán TV2 (ATO 200 CP @29,8 ngày 22/05/2026), lệnh "Hủy mua" TCB 200 bỏ. **Chỉ hai lệnh ATO/ATC được mở rộng nên chỉ hai lệnh có ngày thật**; các lệnh còn lại nằm giữa hai mốc theo thứ tự danh sách (mới → cũ) ⇒ `STOCK_HISTORY_IMPORT` lấy **điểm giữa khoảng** và ghi chú "ngày ước lượng (giữa … và …)" vào `note` của transfer — ngày không ảnh hưởng giá vốn. Giá khớp coi bằng giá đặt với lệnh giới hạn. Số CP cuối: BID 100 · DPR 200 · PHR 80 · VIX 300 · TCB 70 · TV2 0 (đã bán, lỗ hiện thực −1.500k).
  - **Mô hình:** mỗi lệnh là một **transfer** giữa holding tiền "Tiền tại VCBS" (nhóm Tiền, `priceKey:null`) và holding `stk:SYM` — cùng bảng `transfers` với coin, `historyBatch:'vcbs-2026-09'` (`HISTORY_BATCHES.stocks`) chặn nạp trùng. `stockHistoryOps(rows, ids, cashId, batch)` hàm thuần trả `{qty, docs}`; `importStockHistory()` ghi thẳng `store.batch` (holdings + transfers), **không** qua `addTransfer()` — cùng lý do với coin.
  - **Khoản "VCBS" nhập tay cũ (28.950k) đổi thành "Tiền tại VCBS" = giá trị cũ − giá trị thị trường các mã đang giữ** (ước lượng có ghi rõ trong alert; owner sửa theo số dư thật ở Tài sản → Tiền). Nạp **chặn** nếu kho chưa có giá của mã nào đang giữ (DPR/TV2 ngoài VN100 ⇒ thêm `STOCK_EXTRA`/`EXTRA` để crawler cào thêm; `STOCK_ALL` = VN100 + extra dùng cho giá, điểm, backtest, popup Thêm cổ phiếu).
  - **`stockLedgerOf(sym, transfers, holdings)`**: giá vốn **bình quân gia quyền**, bán trừ theo giá vốn TB và ghi lãi/lỗ đã hiện thực — chuẩn kế toán cổ phiếu, **khác coin** ("vốn ròng / số còn giữ"). Card Danh mục cổ phiếu: Mã · Số CP · Giá vốn · Giá · Giá trị · Lãi/lỗ (% trên vốn), tổng; ghi chú một dòng: mã đã bán hết + lãi/lỗ hiện thực + tiền tại VCBS. Bảng cuộn trong card sau 4 dòng (`max-height:155px`) để hàng 2 ~250px và cả tab vẫn một màn (đo 858/860 với 5 mã).
  - **Bộ "Của tôi"** trong `STOCK_SETS` (`syms:null` ⇒ lấy từ holdings `stk:` có số CP > 0 lúc render); trong VN30/VN100 các mã đang giữ có `tr.mine` với dấu ● trước mã. Nút ở Cài đặt → Nâng cao "Nạp lịch sử lệnh VCBS (ảnh 17/09/2026)", tự khoá sau khi nạp. `testStocks` 31 → 40 ca (số CP cuối, 10 docs, chiều bán, ghi chú ước lượng, sổ TV2 −1.500k, VIX vốn TB 24,37k).
  - Chưa nạp: lệnh trước 17/02/2025 (ngoài bộ lọc ảnh), tiền nạp/rút vào VCBS (không có trong ảnh) — nếu owner gửi thêm thì thành mẻ mới với khoá riêng.

- **Chứng khoán PR 5 — mô phỏng kế hoạch owner đề xuất (2026-09-17: "mỗi tháng chọn 1–2 mã điểm 75–85, tháng sau bán khi điểm thấp") + bảng what-if.**
  - **`STOCK_PLAN_V1`** `{entryMin:75, entryMax:100, picks:2, exits:[65,55,25], maxHold:42, minHold:2, fee, sellTax}`. **`simulatePicks(seriesMap, opts)`** hàm thuần mô phỏng theo **danh mục**: phiên đầu mỗi tháng mua tối đa 2 mã điểm cao nhất trong [75,100] chưa giữ, chia đều tiền cho chỗ trống; bán khi điểm ≤ X (sau T+2) hoặc quá 42 phiên; mark-to-market từng phiên ⇒ `netReturn`, `netY1` (equity cuối / equity mốc −365 ngày), `maxDD`, `exits {score,time,end}`, `exposure`. Chuỗi điểm dùng chung với lưới qua **`stockSeriesFor(store, sym)`** (cache `WeakMap` theo object kho — không theo `updatedAt`, hai kho thử nghiệm cùng mốc sẽ dẫm nhau). `stockBacktestAll` trả thêm `plan[3]` + `indexBh/indexBhY1` (VN-Index mua-giữ cùng kỳ); `STOCK_BT_VERSION` v2.
  - **`planWhatIf(store, sym)`**: với mỗi X, `priceForScore()` trực tiếp (không qua `stockBands` — bands chỉ có 4 ngưỡng states, không có 55) ⇒ giá mà điểm về ≤ X, lãi gộp và **lãi sau phí** (mua 0,15%, bán 0,25%). **Tĩnh**: giả định MA200, đỉnh 1 năm, RSI, khối lượng đứng yên — ghi rõ trên UI.
  - Popup Backtest thêm hai khối: bảng kế hoạch 3 dòng (Lệnh · Thắng · Lãi TB/lệnh · Giữ TB · Thoát vì điểm / hết hạn · Toàn kỳ · 12 th · Sụt) + dòng VN-Index mua-giữ, và bảng what-if của mã đang chọn. Ghi chú nói thẳng: **điểm về thấp có hai đường** — giá lên, hoặc đỉnh cũ trôi khỏi cửa sổ 250 phiên (bán mà giá không đổi).
  - **Kết quả thật (kho 17/09/2026, 102 mã, kỳ 21/05/24 → 17/09/26):** VN-Index mua-giữ **+42,7%** · 12 th **+9,1%**; mua-giữ đều 102 mã +15,9% · −13,2%. Kế hoạch: X=65 ⇒ 17 lệnh, thắng 71%, **+20,1% · +24,9%**, sụt 25,7%, giữ TB 13 phiên, 17/17 thoát vì điểm; X=55 ⇒ 13 lệnh, 62%, +22,4% · +19,1%, sụt 25,7%; X=25 ⇒ 12 lệnh, 83%, **+63,5% · +23,4%**, sụt 16,2%, nhưng 9/12 thoát vì **hết hạn 42 phiên** (thực chất là "mua rồi giữ 2 tháng"). Đọc: thắng VN-Index ở 12 tháng gần với cả 3 ngưỡng, toàn kỳ chỉ ngưỡng 25 thắng; 12–17 lệnh là mẫu nhỏ; mã điểm 75+ hôm nay toàn bất động sản/xây dựng rớt sâu (KDH 87, CII 84, DXS 79, PDR 78). What-if hôm nay: CII 12,9k → điểm 65 tại 14,5k (+12,4% sau phí), 55 tại 16,1k (+24,8%); KDH 15,7k → 19,7k (+25,8%) / 22,1k (+41,1%); PDR 11,55k → 12,05k (+3,9%) / 14,25k (+22,9%).
  - `testStocks` 40 → 50 ca (chọn tối đa 2 mã, thoát vì điểm/hết hạn, T+2, chia đều tiền, phí 0,4%, what-if 3 dòng giá tăng theo ngưỡng thấp hơn).

- **Chứng khoán PR 6 — popup "Tính PnL" có thanh trượt điểm + đối chiếu sổ với ảnh Danh mục VCBS (owner yêu cầu 2026-09-17).**
  - **Popup Tính PnL** (`openStockPnl(sym)`; nút "Tính PnL" cạnh Backtest, hoặc **bấm lại** dòng đang chọn trong bảng xếp hạng — bấm lần đầu chỉ chọn mã để còn duyệt bảng được): ô "Mua hôm nay" (nghìn đồng, mặc định vốn trade ở Cài đặt hay 10.000k; gõ `20.000` hiểu là hai mươi nghìn), thanh trượt **điểm bán 0–100** (mặc định `min(65, điểm−5)`, chip 65/55/25 từ `STOCK_PLAN_V1.exits`), 3 ô: số CP (lô 100, đã trừ phí mua) · giá khi điểm về Z · lãi/lỗ sau phí, và bảng 3 mốc cố định + dòng đang kéo. Kéo qua phải quá điểm hiện tại = kịch bản giá xuống (lỗ), ghi rõ trên UI.
  - **Không có thuật toán mới:** `stockPriceAt(store, sym, Z, priceNow)` = `priceForScore()` cắt xuống 0,05k (tách ra dùng chung với `planWhatIf`), nhớ kết quả theo Z trong lúc popup mở (đo: 13 bước kéo ≈ 80 ms). `stockPnlPlan(price0, priceZ, amountK)` hàm thuần: số CP = ⌊tiền / (giá × 1,0015) / 100⌋ × 100, tiền về = CP × giá × (1 − 0,0025). Vẫn **tĩnh** như what-if — ghi chú nói thẳng điểm còn tụt theo thời gian.
  - **Đối chiếu VCBS:** ảnh Danh mục đầu tư 17/09/2026 cho thấy số CP gồm cả **cổ phiếu thưởng/quyền** (`_WFT`: BID +6, PHR +64, VIX +24 và các đợt trước) không có trong lịch sử lệnh, nên sổ mua ra giá vốn TB cao hơn thật (PHR 65,9k vs VCBS 35,833k; VIX 24,37k vs 18,73k). Owner đã sửa tay số CP (khớp VCBS trừ VIX gõ 506, VCBS là 504) nhưng giá vốn không sửa tay được ⇒ `STOCK_RECONCILE_VCBS` `[mã, số CP, giá vốn TB]` + nút một lần ở Cài đặt → Nâng cao "Đối chiếu danh mục VCBS" (`HISTORY_BATCHES.stocksRecon`).
  - **Transfer `kind:'adjust'`**: `toId` = holding, `qtyIn` và `valueVnd` **có dấu** (Δ số CP, Δ vốn), `fromId` null. `stockLedgerOf` cộng thẳng (đếm `adjusts`, không phải mua/bán); `transferImplied` trả null cho dòng này (không có giá ngầm định). `stockReconcileOps(rows, ledgers, holdings, batch, date)` hàm thuần: sổ đã khớp ⇒ không sinh dòng; mã chưa có holding ⇒ `missing` (chặn, bắt nạp lịch sử lệnh trước). `reconcileStocks()` ghi **số CP cho mọi mã** (kể cả đang khớp) + dòng điều chỉnh qua `store.batch`, không qua `addTransfer()`.
  - Kết quả đo trên kho thật sau nạp lịch sử + đối chiếu: tổng vốn **29.463k** · giá trị **24.807k** · lãi/lỗ **−4.656k** — khớp đúng ảnh VCBS (29.463.352đ / 24.806.900đ / −4.656.452đ). Card Danh mục 5 mã cao 247px, tab vẫn 860/860. `testStocks` 50 → 64 ca.

- **Trading PR 7 — tab Trading thành bảng theo dõi 100 coin + 100 cổ phiếu, tab Cổ phiếu chỉ còn phần nắm giữ, sổ lệnh nối vào Tài sản (owner chốt 2026-09-17: "DCA theo dõi hold coin · Cổ phiếu theo dõi hold cổ phiếu · Trading theo dõi list 100 để trading hàng tháng").**
  - **Thứ tự tab:** … DCA · **Chứng khoán** · **Trading** · Cài đặt (`VIEWS`, cả hai nav; `testStocks` có ca kiểm thứ tự). Vẫn 7 tab.
  - **Một khung cho hai thị trường — `MARKETS = {stock, coin}`**, mọi hàm chấm điểm/backtest nhận `store` và đọc cấu hình **gắn trên chính store** (`store.cfg/rules/plan/step`; `cfgOf/stepOf/refRows`) — kho cổ phiếu từ Actions không gắn gì ⇒ mặc định cổ phiếu, test cũ chạy y nguyên. `stockScores(store, syms)`, `stockBacktestAll(store, syms)` nhận danh sách mã; `refRows(store, sym)`: BTC chấm so với ETH (`store.refs`), coin khác so với BTC (`store.index`), cổ phiếu so với VN-Index. `floorPx(p, step)`: cổ phiếu cắt xuống 0,05k, coin cắt theo số chữ số có nghĩa (+1e-9 để giá đã đúng bước không bị cắt thêm một bước vì dấu phẩy động).
  - **Kho coin (`loadCoinStore` → `coinStore`, RAM):** `/api/v3/ticker/24hr` → `pickTopCoins()` (hàm thuần: cặp USDT, bỏ stablecoin/token đòn bẩy/vỏ WBTC-WBETH…, top 100 theo `quoteVolume`) → `klines 1d limit 1000` từng mã (+ BTC, ETH tham chiếu), 6 luồng, mã lỗi vào `errors` không chặn cả kho. `COIN_SCORE_V1` = `BUY_SCORE_V1` của DCA (version `coin-d1-v1`, nhãn thành phần 7 "So với BTC 30 ngày"); `COIN_SWING_RULES` phí 0,1% hai chiều, không T+2, không sàn/trần; `COIN_PLAN_V1` lot 0. Giá vào `D.prices['cx:SYM'] = {value: USD × tỷ giá usdt nhập tay (chưa có ⇒ 0, không đoán), usd, source:'live'}`; `D.prices.coins = {storeAt, ranks, bt}` nhỏ. Mở tab lần đầu ở chế độ Coin tự tải một lần (`coinAutoTried`), nút Cập nhật tải lại. **Chưa gọi được Binance thật từ sandbox** — đo bằng Binance giả (102 lệnh klines, 100 mã có điểm trong 0,6 s; backtest 100 coin ≈ 6 s).
  - **Tab Trading** (`renderTrade`, id `td*`): seg **Cổ phiếu / Coin** (`state.tradeMkt`), hero + phân rã của mã đang chọn (`state.tradeSym[mkt]`), bảng xếp hạng (cổ phiếu: VN30/VN100/Của tôi; coin: Top 100/Của tôi — "Của tôi" = holding có prefix của thị trường đó, số lượng > 0, dấu ●), Vùng giá mua, **Sổ lệnh**. Nút: **Mua / Bán · Tính PnL · Backtest · Cập nhật**. `fillHero(prefix, o)` và `bandTableHtml()` dùng chung với tab Cổ phiếu. Bấm dòng chọn mã, bấm lại ⇒ Tính PnL. `openBacktest(M)` và `openPnl(M, sym)` là bản tổng quát của popup cổ phiếu cũ (cache backtest `D.prices[M.btKey].bt`).
  - **Tab Cổ phiếu** (`renderStocks`) chỉ còn: hero của **mã đang giữ** (bấm dòng Danh mục để chọn; meta ghi giá vốn và % so giá vốn), Danh mục cổ phiếu (thêm cột Điểm, card cao bằng hero, cuộn trong card), Vùng giá mua, **Lịch sử giao dịch** (mọi transfer chạm holding `stk:` — mua/bán/điều chỉnh, nhãn `trade` nếu ghi từ Trading). Danh sách 100 mã, Backtest, Tính PnL **dời hết sang Trading**.
  - **Popup Tính PnL có dòng ĐỘNG:** `scoreAfterFlat(store, sym, n)` nối n nến phẳng (giá = nến cuối, khối lượng = trung vị `volLong` phiên) vào cả mã và tham chiếu rồi chấm lại ⇒ điểm tụt **chỉ vì thời gian trôi** (đỉnh cũ ra khỏi cửa sổ, MA200 hạ, RSI hồi). `scoreDecayPath(store, sym, 63)` tính một lần khi mở popup (64 lần chấm), `decayFirstAt(path, Z)` = phiên đầu điểm ≤ Z. Dòng hiện "điểm sau 10/21/42/63 phiên" và "về ≤ Z sau N phiên ≈ ngày mà giá không cần lên ⇒ bán lúc đó chỉ mất phí". Đo thật CII 17/09: 84 → 77 (10) → 62 (21) → 58 (63); về ≤ 65 sau 17 phiên. Bảng và ba ô vẫn là tĩnh, ghi chú nói rõ hai phần khác nhau thế nào. Coin: tiền nhập nghìn đồng, tính bằng USD qua tỷ giá usdt, lot 0 ⇒ số coin lẻ (`stockPnlPlan(..., {lot:0})`).
  - **Sổ lệnh = transfer thật + nhãn `trade`.** `openTradeOrder(M, sym, side)`: Mua/Bán, số lượng (cổ phiếu ép lô 100), giá (mặc định giá hiện tại, sửa được), tiền lấy từ/về (cổ phiếu: holding Tiền, mặc định "Tiền tại VCBS"; coin: holding `usdt`). Ghi qua **`addTransfer()`** với `trade:{mkt, sym, side, qty, px}` (+`usdtRate` cho coin) ⇒ số dư holding, tài sản ròng, Danh mục cổ phiếu, Ví Coin, dòng Binance **tự đúng**, không có state riêng. Cổ phiếu dùng chung holding `stk:SYM` với Danh mục (sổ `stockLedgerOf` đọc luôn); coin tạo holding riêng **`cx:SYM` tên "SOL (trade)"** nhóm Crypto — không đụng ví DCA (`COIN_FAMILY`/`familyHoldings` lọc theo `priceKey` nên `cx:` không lọt vào giá vốn DCA). `isCoinKey(k)` (khoá live **hoặc** `cx:`) thay `LIVE_KEYS.includes` ở mọi chỗ gộp coin (dòng Binance, Ví Coin, `cryptoUsd`). Bán vượt số đang giữ ⇒ chặn; `addHolding` trả `id`.
  - **`tradeLedger(transfers)`** hàm thuần: chỉ đọc transfer có `trade.sym`, giá vốn bình quân gia quyền **theo đơn vị giá** (k/USD) **và theo nghìn đồng**, bán ghi `pnlPx`/`pnlK`/`ret`; trả `positions`, `closed`, `realizedK`, `wins`, `n`. Card Sổ lệnh: 3 ô (đang mở tạm tính · đã chốt tháng này · đã chốt tổng + tỷ lệ thắng) + bảng vị thế (Mã · Điểm · SL · Giá vốn · Giá · Lãi/lỗ · nút Bán) + popup **Lịch sử (N)**. Card cao bằng Vùng giá (cùng cơ chế `height:0;min-height:100%`), bảng cuộn trong card.
  - **Nối vào Phân rã tăng trưởng (Tổng quan):** lệnh trade là Chuyển đổi nên **đã nằm** trong Thị trường/Khác của phép phân rã — không cộng thêm. `tradeStatsIn(p)` (kỳ theo `S0.snapAsOf`/cuối tháng S0 → `S1.snapAsOf`/hôm nay) chỉ **tách ra một dòng ghi chú** dưới biểu đồ: "Trading trong kỳ: đã chốt ±X (N lệnh, thắng %) · N vị thế mở tạm tính ±Y — đã nằm trong các cột trên", hiện cả khi chưa có mốc so sánh. Chưa làm cột riêng trong waterfall (sẽ trùng với Thị trường ở vị thế giữ qua đầu kỳ).
  - Đo: hai tab đều 2×2, 4 card `481/481/222/222` ở 1920×860 và 1440×900 (cả hai thị trường), không cuộn trang; điện thoại 390 không tràn ngang; console sạch. `testStocks` 64 → 83 ca (thứ tự tab, `pickTopCoins`, `floorPx`, `isCoinKey`, kho coin dùng `refs`, `scoreAfterFlat`/`scoreDecayPath`, `stockPnlPlan` lot 0, `tradeLedger` 2 thị trường).

- **Rà soát trùng lặp lần 2 + đồng bộ hai tab nắm giữ (owner yêu cầu 2026-09-17 sau PR 7: "lược bỏ hết nội dung/thao tác trùng, bố cục đối xứng, hai tab coin và cổ phiếu xem cùng một kiểu").**
  - **Khung chung cho hai tab nắm giữ (DCA và Chứng khoán) — giống nhau từng ô:** nút góc trên `Lịch sử (N) · Thêm · Mua nhanh · Cập nhật`; hàng 1 = card **Buy Score** (tiêu đề `.colhead` + hero + meta + Phân rã 7 dòng) | card **Giá vốn & Ví Coin / & Danh mục** (bộ chọn tài sản `.seg` trên tiêu đề, 3 ô tight Đang giữ · Giá vốn TB · Lãi/lỗ, bảng danh mục, thanh tỉ trọng `.catstack` + `.catlegend`); hàng 2 = **Vùng giá mua** | **Tháng này** (một dòng gợi ý + bảng 3 dòng). Đo: cả hai tab `481/481/244/244` ở 1920×860 và 1440×900. DCA chỉ thêm tiêu đề "Buy Score · nến ngày · cửa sổ 365 ngày · so với BTC" cho card hero (trước đây thiếu nên card 441 lệch với hai tab kia).
  - **Chứng khoán:** bộ chọn mã đang giữ `#stockSymPick` (như bộ chọn coin của DCA; bấm dòng Danh mục cũng chọn), 3 ô `#stockStats` cho mã đang chọn, thanh tỉ trọng `#stockAlloc`. Card **"Tháng này"** cho cổ phiếu = `stockPlanNow(ranks, mine, transfers, month)` (hàm thuần, cùng luật `STOCK_PLAN_V1` mà Backtest mô phỏng): dòng *Mua mới* liệt kê mã điểm ≥ 75 chưa giữ (tối đa `picks`) và số lệnh mua tháng này, *Đang giữ* với dải điểm và mã đã về ≤ 65 ("nên bán" theo luật), *Đã bán* với lãi/lỗ chốt trong tháng; `stockSuggestion()` một dòng như `dcaSuggestion()`. **Lịch sử giao dịch thành popup** `openStockHistory()` (trước là card hàng 2 — trùng với Lịch sử chuyển đổi ở Tài sản). Nút **Mua nhanh** gọi thẳng `openTradeOrder(MARKETS.stock, sym, 'buy')` của Trading — một form Mua/Bán cho cả app.
  - **Tài sản:** nhóm **Chứng khoán gộp một dòng** như Binance (`stkRow`: N mã · lãi/lỗ theo sổ · tổng · link CK) — số CP/giá vốn quản ở tab Chứng khoán, không sửa tay ở đây để không lệch sổ; form Thêm tài sản và ô nhóm bỏ `Chứng khoán` (thêm cổ phiếu ở tab Chứng khoán). **Lịch sử chuyển đổi thành card hàng 2 chạy hết phần trống** (tab trước đây trống nửa màn vì Chi phí vốn ẩn): `#view-taisan.active` flex cột cao `calc(100vh − 52px)` như Dòng tiền, `#xferCard` nhận phần còn lại, bảng cuộn trong card; nút popup "Lịch sử chuyển đổi" bỏ. Đây là **sổ gốc duy nhất** của mọi lần đổi tài sản (mua coin, lệnh cổ phiếu, trade, đối chiếu) và là nơi duy nhất xoá được một dòng — các "Lịch sử" ở DCA/Chứng khoán/Trading chỉ là cách đọc lại theo từng tài sản.
  - Giữ nguyên (không phải trùng): Vùng giá mua ở cả ba tab (tab nắm giữ soi mã đang giữ, Trading soi mã đang duyệt); ba nút Cập nhật (mỗi tab làm tươi dữ liệu của chính nó — DCA: điểm ETH/BTC; Chứng khoán: kho; Trading: thị trường đang chọn). Trading hàng 2 (`222`) thấp hơn hai tab nắm giữ 22px vì Sổ lệnh dùng `height:0` để bảng cuộn trong card — chấp nhận, không pin pixel.
  - `testStocks` 83 → 88 ca (`stockPlanNow`: ứng viên, ngưỡng bán, đếm lệnh tháng theo thị trường; `stockSuggestion`; rỗng không lỗi).

- **Trang bị phóng to trên iPhone khi tải, chạm đôi, mở popup (owner báo 2026-09-17).** Ba nguyên nhân, ba chốt — giữ nguyên, đừng gỡ:
  - **Ô nhập < 16px**: iOS tự zoom khi focus vào ô chữ nhỏ hơn 16px (chữ nền app 14px, bảng compact 12px). Popup `showModal` tự focus ô đầu ⇒ zoom ngay khi mở; form Dòng tiền/Chuyển đổi `focus()` sau khi thêm ⇒ zoom. Sửa: `@media (hover:none) and (pointer:coarse){ input, select, textarea, input.inline { font-size:16px } }` — chỉ ô nhập, không đụng bảng/nút; và `showModal` **không tự focus** trên thiết bị cảm ứng (`matchMedia('(pointer:fine)')`), Enter = OK vẫn giữ.
  - **Chạm đôi**: `html, body { touch-action: manipulation }` (Safari iOS 13+ và WKWebView đều tôn trọng).
  - **Viewport**: thêm `maximum-scale=1, user-scalable=no`. WKWebView trong vỏ Capacitor **tôn trọng** (hết pinch/zoom lệch), Safari bỏ qua nhưng không hại. Đo bằng Playwright iPhone 13: ô nhập 16px, popup không focus, không tràn ngang.

- **Đầu tab đồng nhất (owner yêu cầu 2026-09-17 từ ảnh iPhone).** Bỏ mọi `<small>` giải thích trong `.vhead .right` (mốc so sánh Tổng quan, "đã chốt" Dòng tiền, kho/Binance ở Chứng khoán/Trading, cảnh báo thiếu giá ở DCA) — thời gian kho/Binance chuyển vào góc phải card Buy Score (`fillHero` nhận `o.src`), các thông tin còn lại đã có chỗ khác (ghi chú dưới biểu đồ, Bảng tháng, toast). Bỏ số đếm trên nút (`Lịch sử (23)`, `Bảng tháng (21)`, `Lịch sử (N)` ở Sổ lệnh). Bộ chọn Cổ phiếu/Coin của Trading chuyển vào colhead card Buy Score. **Nút đầu tab cùng cỡ, một hàng:** desktop `.vhead .right .btn{width:128px}`; điện thoại `.vhead` xếp dọc (tiêu đề trên, hàng nút dưới), nút `flex:1 1 0` chia đều, chữ 12px, không xuống hàng — đo iPhone 13: 4 nút 86px cùng một hàng ở DCA/Chứng khoán/Trading. "Thêm cổ phiếu" đổi thành "Thêm mã" cho vừa. `.colhead` thêm `gap` + `flex-wrap` (tiêu đề DCA từng dính vào chữ version trên điện thoại).

- **Xác nhận hồi tách khỏi Buy Score + nghiên cứu sự kiện (owner hỏi 2026-09-17: "score chỉ theo giá, càng rớt càng cao nhưng bão hoà ở 100 — sao biết đáy hay còn rớt; giá hồi thì score tụt, nghịch lý khi mua").** Trả lời đã ghi: Buy Score đo **độ rẻ**, không đo **đã ngừng rớt chưa**; hai việc kéo ngược chiều nên KHÔNG nhồi vào một điểm.
  - **`confirmSignal(candles, i)`** hàm thuần, `CONFIRM_V1 = {ma:20, slope:3, lowWin:10, volWin:5, need:2, waitBars:21}`: ba dấu độc lập với điểm — đóng cửa trên MA20 và MA20 đang lên · đáy 10 phiên gần cao hơn đáy 10 phiên trước · 5 phiên gần tăng và khối lượng phiên tăng > phiên giảm; `ok` khi ≥ 2/3. `stockScoreOf` trả kèm `confirm`; bảng xếp hạng Trading có cột **Hồi** (`✓ 2/3` đậm khi ok, mờ khi chưa); card "Tháng này" của Chứng khoán đánh ✓ sau ứng viên có dấu hồi và ghi "(chưa mã nào có dấu hồi)" khi không có.
  - **`eventStudyOne/All`** hàm thuần (`EVENT_V1 = {thresholds:[75,80,85], horizons:[21,42,63], rearm:10}`): sự kiện = điểm vượt lên ≥ T (phải tụt dưới T−10 mới tính lần mới); đo lợi suất sau 21/42/63 phiên và **rớt thêm tối đa** trong 63 phiên; hai cách vào: `now` (ngay khi chạm) và `confirm` (chờ tới phiên đầu có ≥ 2/3 dấu trong 21 phiên, điểm còn ≥ T−10, không có ⇒ bỏ). Bảng "Sau khi điểm chạm mốc" trong popup Backtest (6 dòng), `stockBacktestAll` trả `events`, `STOCK_BT_VERSION` v3. Chưa tính phí, chưa tính T+2 — để so hai cách vào với nhau, không phải để ra lãi.
  - **Kết quả thật (kho 17/09/2026, 102 mã, 235 sự kiện ≥ 75):** vào ngay ⇒ rớt thêm trung vị **−7,7%**, 10% xấu nhất **−22,5%**, sau 63 phiên dương 73% · trung vị +9,3%. Chờ xác nhận ⇒ chỉ còn **23 sự kiện** (chờ TB 15,7 phiên), rớt thêm trung vị **−0,2%**, 10% xấu −12,8%, sau 63 phiên dương 83% · trung vị **+25,0%**. Cùng chiều ở 80 và 85 (85: 136 → 6 sự kiện). Đọc đúng: xác nhận hồi lọc được phần lớn dao rơi và cải thiện cả tỉ lệ dương lẫn trung vị, **nhưng** mẫu nhỏ (23/16/6) và có thiên lệch sống sót (chỉ giữ sự kiện mà giá quay đầu trong 21 phiên) ⇒ đủ để dùng làm **điều kiện thứ hai** khi trade (điểm ≥ 75 **và** Hồi ✓), chưa đủ để nói lợi thế ổn định. Hôm nay 27/100 mã có dấu hồi; 7 mã điểm cao nhất (KDH 87 … VSC 77) đều 0/3 — đúng kịch bản "rẻ nhưng còn rớt". DCA giữ nguyên chỉ dùng điểm.
  - `testStocks` 88 → 98 ca (3/3 trên mã hồi, 0/3 trên mã rớt đều, thiếu nến, 2 sự kiện với rearm, bỏ sự kiện gần cuối, chờ xác nhận bỏ sự kiện 1 vào sự kiện 2, 6 dòng tổng hợp, quantile).

- **Bộ lọc "Gợi ý" (owner yêu cầu 2026-09-17: "dựa theo tổng hợp các chỉ báo kèm bộ lọc để đưa ra gợi ý" — phép lọc trong app, không phải khuyến nghị của ai).** `screenPicks(ranks, mine, P)` hàm thuần, `SCREEN_V1 = {entryMin:75, needConfirm:2, maxDd:0.65, lowConf:'LOW'}`: **MUA** = điểm ≥ 75 **và** Hồi ≥ 2/3 **và** độ tin cậy không thấp **và** rớt từ đỉnh 1 năm ≤ 65% (sâu hơn là rẻ có thể vì doanh nghiệp hỏng, điểm không biết); **THEO DÕI** = điểm ≥ 75 nhưng thiếu dấu hồi; **LOẠI** ghi lý do (đang giữ · độ tin cậy thấp · rớt quá sâu). Xếp Hồi nhiều trước rồi điểm. Bộ **"Gợi ý"** đứng đầu bộ chọn của cả hai thị trường (`STOCK_SETS`/`COIN_SETS` key `PICK`) và là **mặc định** khi mở Trading; dòng meta ghi số mua/theo dõi/loại kèm luật; nhãn `mua`/`theo dõi` cạnh mã; cột Mã nới 19% để chứa nhãn. Card "Tháng này" (Chứng khoán) dùng cùng hàm: mua xếp trước, theo dõi sau; `stockSuggestion` nói "chưa mã nào đủ điều kiện mua · theo dõi: …" khi chưa có dấu hồi. Kết quả thật 17/09: **0 mua · 9 theo dõi** (DGC, PNJ 1/3; KDH, CII, DXS, PDR, NKG, VCG, VSC 0/3). `testStocks` 98 → 103 ca.

- **Nhãn "rẻ kiểu gì" + sự kiện tách theo nhãn (owner chốt 2026-09-17: "gắn nhãn trước, đo sau, đủ khác biệt mới nâng thành điều kiện" — làm bước 1 và 2, bước 3 chờ owner).**
  - **`cheapKind(components)`** hàm thuần, `KIND_V1`: nhóm chậm = giảm từ đỉnh 1 năm + lệch MA200 + percentile (tối đa 55) · nhóm nhanh = giảm từ đỉnh tháng + bán tháo + RSI (tối đa 40); so **tỉ lệ đạt** hai nhóm, lệch ≥ 0,15 ⇒ `struct` ("cấu trúc") hay `panic` ("hoảng loạn"), còn lại `mixed`. `swingScoreSeries` gắn `kind` vào từng nến (đã có components sẵn, không thêm lần chấm), `stockScoreOf` trả `kind`. Hiện dạng chữ nhỏ cạnh nhãn mua/theo dõi trong bộ Gợi ý và trong meta hero Trading ("rẻ kiểu cấu trúc · hồi 2/3"). **Không phải điều kiện lọc.**
  - `eventStudyAll` mỗi dòng có `byKind` (kind tại nến chạm mốc); popup Backtest thêm bảng "Theo kiểu rẻ" ở ngưỡng vào (75): 3 kiểu × 2 cách vào. `STOCK_BT_VERSION` v4.
  - **Kết quả thật 17/09 (ngưỡng 75, vào ngay):** *hoảng loạn* 42 sự kiện ⇒ rớt thêm trung vị −6,7%, sau 63 phiên **dương 90% · trung vị +24,4%**; *cấu trúc* 91 sự kiện ⇒ rớt thêm −9,9% (10% xấu −27,1%), sau 63 phiên **dương 57% · +2,5%**; hỗn hợp 102 ⇒ 75% · +9,0%. Chờ xác nhận: cấu trúc 11 sự kiện ⇒ −1,8% / 83% / +26,1% (xác nhận cứu được kiểu cấu trúc); hoảng loạn chỉ 1 sự kiện (hồi quá nhanh, điểm tụt dưới 65 trước khi đủ dấu ⇒ cổng xác nhận không hợp kiểu này). Cả 9 mã "theo dõi" hôm nay đều là **cấu trúc** — đúng nhóm tệ nhất khi vào ngay. Gợi ý bước 3 (chưa làm, chờ owner): luật theo kiểu — hoảng loạn có thể vào ngay khi ≥ 75, cấu trúc bắt buộc chờ Hồi ≥ 2/3; mẫu cấu trúc-chờ mới 11 nên vẫn là giả thuyết.
  - `testStocks` 103 → 108 ca.

- **Bước 3 — nhãn thành điều kiện vào + so ba cách vào với bản cũ (owner chốt 2026-09-17: "làm bước 3 và so với bản cũ").**
  - **`ENTRY_RULES`** ba luật: `score` (cũ: điểm hôm nay ≥ 75), `confirm` (điểm + Hồi), `kind` (theo kiểu rẻ: **hoảng loạn vào ngay khi ≥ 75**; cấu trúc/hỗn hợp đi theo cách confirm). Cách confirm **không** đòi điểm ≥ 75 và Hồi cùng một phiên — điểm tụt khi giá hồi nên hai điều kiện đó gần như loại trừ nhau (bản đầu ra 0 lệnh). Viết lại đúng cách "chờ xác nhận" của bảng sự kiện: **đã chạm 75 trong 21 phiên gần nhất (`peak`), hôm nay còn ≥ 65 (75 − `rearm`), Hồi ≥ 2/3.** `entryOk(rule, {score, peak, kind, conf}, P)` hàm thuần dùng chung cho bộ Gợi ý và `simulatePicks`.
  - Bộ Gợi ý (`SCREEN_V1.entryRule = 'kind'`): tập xét mở rộng thành "điểm ≥ 75 **hoặc** đã chạm 75 trong 21 phiên và còn ≥ 65" (`stockScores` gắn `peak21` cho mã ≥ 65) ⇒ theo dõi tăng 9 → **20** mã hôm nay, vẫn **0 mua**, tất cả kiểu cấu trúc. `simulatePicks(seriesMap, {entryRule})` tính `peak` trượt 21 nến trước khi chọn; `stockBacktestAll` trả `plan` (cũ, luật score) + **`planCmp`** = 3 luật × 3 ngưỡng bán. `STOCK_BT_VERSION` v5. Popup Backtest: bảng "Kế hoạch chọn mã hằng tháng — ba cách vào" (9 dòng, nhóm theo luật) + dòng **`planCmpNote()`** tự đọc kết quả (nói thẳng khi dưới 10 lệnh).
  - **Kết quả thật 17/09 (102 mã, 21/05/24 → 17/09/26; VN-Index +42,7% · 12 th +9,1%):** luật cũ X=65 17 lệnh +20,1%/+24,9% sụt 25,7% (X=25: 12 lệnh +63,5%/+23,4% sụt 16,2%); **điểm + Hồi chỉ 3 lệnh** (X=65: −2,3%/−4,0% sụt 5,2%; X=25: +11,9%/−4,0%); **theo kiểu rẻ 4 lệnh** (X=65: −0,5%/−2,3% sụt 5,7%; X=25: +19,3%/+2,3% sụt 7,1%). **Đọc đúng:** hai cách có Hồi ra rất ít lệnh **không phải vì thị trường thiếu cơ hội** (bảng sự kiện đếm 23 lần chờ xác nhận) mà vì kế hoạch chỉ **mua đầu tháng**, còn dấu hồi xuất hiện ở phiên bất kỳ và tắt trước đầu tháng sau. Dưới 10 lệnh thì lãi/thua chưa nói được gì; điều thật duy nhất là sụt tối đa 5–7% so 26%. **Chưa kết luận được luật mới tốt hơn luật cũ.** Bước kế (chưa làm, chờ owner): cho kế hoạch soi từng phiên (hoặc mỗi tuần) thay vì đầu tháng để so công bằng — đây là đổi nhịp mua, không phải đổi luật.
  - `testStocks` 108 → 119 ca.

- **Sửa theo audit ngoài (GPT6 Astra, 2026-09-17; owner: bỏ F01 quyền riêng tư, ưu tiên phần chỉ số/chỉ báo/gợi ý).** Đã đối chiếu 12 phát hiện với code; các phát hiện về phân tích đều ĐÚNG, sửa trong một PR. Nguyên tắc rút ra, giữ cho mọi lần sau:
  - **Chỉ chấm điểm trên nến ĐÃ ĐÓNG (F04).** `parseKlines(rows, now)` bỏ nến có `closeTime > now`, trả nến đang chạy ở thuộc tính không đếm `.live`; kho coin giữ `symbols[SYM].live` chỉ để hiện giá/`% hôm nay`. Mốc dữ liệu DCA = lúc nến cuối đóng. Điểm coin do đó là điểm của ngày hôm qua đã khép, không đổi trong ngày — đúng chủ ý.
  - **Tuổi dữ liệu TỪNG MÃ vào Confidence (F03).** `dataAgeHours(rows, isStock, now)`: cổ phiếu tính từ 15:00 VN của nến cuối, bỏ thứ Bảy/Chủ nhật (kho cào thứ Sáu ⇒ sáng thứ Hai vẫn mới; sáng thứ Ba chưa cào ⇒ 42 giờ > 30 ⇒ LOW); coin từ `closeTime`. `stockScoreOf` truyền `ageHours` và trả `ageH, stale`; hero gắn "dữ liệu cũ" theo mã; bộ Gợi ý loại với lý do `dữ liệu cũ`.
  - **MỘT hàm quyết định cho live và backtest — `entryDecision(b, P)` (F02).** Gom "đang xét" (điểm ≥ 75 hoặc vừa chạm trong 21 phiên) + lý do loại (dữ liệu cũ · độ tin cậy thấp · rớt > 65%) + `entryOk` theo luật. `screenPicks` và `simulatePicks` đều gọi nó; chuỗi điểm (`swingScoreSeries`) mang thêm `open, confidence, dd` để backtest có đủ đầu vào. Trước đó backtest chỉ lọc điểm nên 17/17 lệnh của "kế hoạch cũ" không qua được bộ lọc live.
  - **Khớp lệnh (F05):** tín hiệu ở giá đóng phiên t, **khớp ở giá MỞ phiên t+1** (`execAt:'close'` giữ làm đối chứng); không mua khi mở cửa sát trần, không bán khi mở cửa sát sàn (`floorPct` lấy từ luật của thị trường, coin 0); **vị thế còn mở ở nến cuối không ép bán** — vào equity theo giá cuối, đếm ở `exits.end`, không vào `n`/tỷ lệ thắng (`entries` = số lần mua). `cadence:'day'` soi từng phiên như bộ Gợi ý với tối đa `picks` lệnh mua mới mỗi tháng; `plan` (cũ) vẫn `cadence:'month'`; `planCmp` chạy `day`. "Giữ N phiên" giờ tính giữa hai lần khớp nên T+2 ra 3 (tín hiệu bán ở phiên T+2, khớp T+3).
  - **Sổ lệnh không bịa giá vốn (F06):** bán nhiều hơn số đã mua qua Trading ⇒ dòng `basis:false`, lãi/lỗ `null`, hiện "chưa xác định", không cộng vào hiện thực, đếm `unknown`. Bán mã mua ở nơi khác thì xem lãi/lỗ ở sổ tab Chứng khoán/DCA.
  - **Cache backtest theo kho (F07):** `stockBacktestAll` trả `storeAt`; `btCacheOf` bỏ cache khi `storeAt` khác `updatedAt` của kho đang có. Ghi chú dưới bảng nói thẳng: 9 bộ chọn và đánh giá trên cùng dữ liệu, rổ theo danh sách hiện tại ⇒ kết quả dễ đẹp hơn thật (chưa kiểm định ngoài mẫu — việc sau).
  - **Bảng sự kiện (F08):** rớt thêm chỉ đo khi còn đủ 63 phiên (`nDd`), mọi ô lợi suất hiện `(n)`; tiêu đề ghi rõ đo theo giá đóng, sự kiện cùng ngày không độc lập. `simulateLadder` **không vào nhịp 2 sau khi đã bắt đầu thoát** (F12 — trước đó có thể dùng tiền vừa chốt lời để mua lại rồi bán ngay cùng nến vì cờ hoà vốn). `parseStockStore` lọc số hữu hạn, khối lượng âm, xếp thời gian, gộp trùng ngày (F10). `STOCK_BT_VERSION` v6.
  - **Chưa làm (ghi để không quên):** F01 dữ liệu cá nhân trong repo public (owner tạm bỏ); F07 rổ mã theo thời điểm (không có nguồn thành phần lịch sử VN100/top-100 Binance); F10 khai báo đơn vị theo provider và cờ adjusted/raw trong crawler (không kiểm chứng được từ sandbox).

- **Đợt 2 theo audit (cùng ngày, owner: "làm luôn những việc đúng mà chưa làm").**
  - **Bậc chốt lời/cắt lỗ/hoà vốn/nhịp 2 của `simulateLadder` soi theo ĐÁY và ĐỈNH trong nến** (chuỗi điểm mang `high, low`; chuỗi giả không có ⇒ dùng close, `testSwing` 70 ca giữ nguyên). Một nến chạm cả hai ⇒ tính **cắt lỗ trước** (không biết cái nào trước thì chọn bất lợi). Giá khớp = mốc, hoặc giá mở nếu nến mở cách xa mốc (gap). `rules.execAt:'nextOpen'` (đặt trong `STOCK_SWING_RULES`, `COIN_SWING_RULES`) ⇒ tín hiệu vào ở giá đóng, mua ở giá MỞ nến sau; `SWING_RULES_V2` (lưới H4 cũ) giữ 'close'. Nhịp 2 không vào cùng nến với nhịp 1.
  - **`walkForward(store, syms)` — kiểm định ngoài mẫu đơn giản (F07):** chia đôi kỳ theo thời gian, chọn bộ/luật tốt nhất trên nửa đầu, đo trên nửa sau; kèm "nếu biết trước" (tốt nhất nửa sau) để thấy khoảng lạc quan. Trả `wf` trong `stockBacktestAll`, bảng 2 dòng (lưới · kế hoạch) trong popup Backtest. Rổ vẫn là danh sách hiện tại.
  - **Nhãn "Độ tin cậy" → "Dữ liệu: đủ / thiếu tham chiếu / thiếu hoặc cũ"** (`confLabel`, F09) — nói về chất lượng dữ liệu, không phải xác suất lời; lý do loại của Gợi ý đổi thành "dữ liệu thiếu hoặc cũ". **DCA không mở Smart/Opportunity trên điểm quá 48 giờ** (`dcaPlanNow` truyền `score=null` khi `dcaStale`, cờ `staleBlocked`, dòng gợi ý nói "bấm Cập nhật").
  - **Workflow crawler** thêm `permissions: pages: write`, bước gọi API `POST /repos/…/pages/builds` sau khi push (F11: push bằng GITHUB_TOKEN không được bảo đảm kích Pages) và bước **kiểm chứng** kho trên Pages đã đúng `updatedAt` mới (chờ tối đa 3 phút, chỉ `::warning::` nếu không). Chưa chạy thật từ sandbox — lần Actions kế tiếp sẽ cho biết.
  - **Kho coin tải kèm coin đang giữ** (`loadCoinStore(onProgress, extraSyms)` với `mktMine(MARKETS.coin)`) dù đã rớt khỏi top 100 (F12). **Form Mua/Bán:** mua vượt số dư khoản tiền đang chọn ⇒ chặn; cảnh báo (không chặn) khi đang mở ≥ `SWING_SIZING_V1.maxOpen` vị thế hoặc tháng này đã chốt lỗ ≥ 10% vốn trade; ghi rõ phí/thuế không tự trừ (F06).
  - **Bảng sự kiện** cột "chờ xác nhận" ghi thêm **bỏ qua N** (sự kiện không có dấu hồi trong 21 phiên) và **vào ±x% so lúc chạm** (chi phí chờ, trung vị) — F08.
  - `STOCK_BT_VERSION` v7. `testStocks` 134 → 142 ca.
  - **Kết quả kho thật 17/09 sau đợt 2:** lưới 9 bộ với khớp mở phiên sau + đáy/đỉnh: bộ tốt nhất 75/L3 +4,5%/+0,6% (trước: 75/L2 +4,8%/+1,7%) — vẫn thua mua-giữ TB +15,9% toàn kỳ. Kế hoạch (không dùng high/low, không đổi so đợt 1). **Kiểm định ngoài mẫu (21/05/24 → 19/07/25 chọn, 19/07/25 → 17/09/26 đo):** lưới chọn 55/L3 (nửa đầu +4,0%) ⇒ nửa sau **−1,8%**, mua-giữ TB nửa sau −2,3%, biết trước 75/L3 +0,7%; kế hoạch chọn "điểm + Hồi, X=25" (nửa đầu +17,9%) ⇒ nửa sau **−17,7%**, 3 lệnh, sụt 33%; VN-Index nửa sau **+22,7%**; biết trước là "cũ đầu tháng X=65" +27,6%. **Đọc:** luật chọn theo nửa đầu không giữ được ở nửa sau — bảng chính lạc quan hơn thật đúng như audit nói; chưa có luật nào đáng tiền thật, kể cả "điểm + Hồi".
  - **Kết quả kho thật 17/09 SAU sửa (102 mã, 21/05/24 → 17/09/26; VN-Index +42,7% · 12 th +9,1%):** cách cũ (điểm ≥ 75, đầu tháng) X=65: 15 lệnh 53% +19,2%/+27,6% sụt 25,6%; X=25: 8 lệnh +42,2%/+25,2% sụt 22,3% (trước sửa +63,5% — khớp mở phiên sau và 2 vị thế mở cuối kỳ lấy đi phần chênh). **Cùng soi từng phiên:** điểm ≥ 75: 32 lệnh **−18,4%/−10,7%** sụt 32% (vào ngay mỗi lần điểm chạm 75 = bắt dao rơi liên tục); điểm + Hồi X=65: 10 lệnh **thắng 90%, +14,6%/+0,2%, sụt 5,2%**, X=55: +17,3%/+3,7% sụt 18,7%; theo kiểu rẻ: 15–16 lệnh +4,9…6%/−1,2…−1,8% sụt 21,6% (phần "hoảng loạn vào ngay" kéo xuống). Bảng sự kiện sau sửa: ngưỡng 75 vào ngay n=235, đủ 63 phiên 181, rớt thêm trung vị −7,3%, 63 phiên 73%/+9,3%; chờ xác nhận n=23, đủ 63 phiên 18, rớt thêm 0,0%, 83%/+25,0%. **Đọc:** không cách nào thắng VN-Index mua-giữ toàn kỳ; luật "điểm + Hồi" là cách duy nhất sụt nhỏ và thắng cao nhưng 10 lệnh và 12 tháng gần ≈ 0; luật theo kiểu rẻ KHÔNG tốt hơn điểm + Hồi trên dữ liệu này ⇒ **không nâng "hoảng loạn vào ngay" thành mặc định**. Gợi ý hôm nay vẫn 0 mua · 20 theo dõi.

- **Crawler chạm timeout 15 phút, hai lần liên tiếp không commit gì (17/09 13:45 và 15:14).** Đọc log: **VCI chặn tốc độ sau ~40 mã** rồi timeout mọi mã còn lại; bốn nguồn dự phòng đều chết từ máy Actions (TCBS 403 · SSI 403 · VNDirect lỗi mạng · CafeF 0 nến) nên **mỗi mã hỏng tốn 33 giây cho một kết quả đã biết trước**. Job bị GitHub huỷ ⇒ bước commit không chạy ⇒ **kho không đổi dù đã chạy 15 phút**. Bốn sửa, giữ nguyên tắc "không bao giờ mất nến đã có":
  - **Chỉ xin phần THIẾU.** Kho đã có ≥ 300 nến thì `want = số ngày hở + 5` (tối thiểu 10, tối đa 800) thay vì tải lại 800 nến × 103 mã mỗi ngày — chính thứ làm nguồn chặn tốc độ. Mã mới hoặc kho ngắn vẫn tải đủ 800.
  - **Thử lại nguồn chính thay vì rơi xuống nguồn đã chết.** Mỗi nguồn có `timeout` và `retries` riêng: VCI 8 giây × 3 lần (chập chờn, thử lại rẻ), còn lại 10 giây một lần.
  - **Nguồn hỏng 5 lần liên tiếp ⇒ bỏ nốt lần chạy** (`dead`), mã sau không phải chờ lại. Hỏng 8 mã liên tiếp ⇒ nghỉ 45 giây rồi cho mọi nguồn sống lại, tối đa 2 lần — đúng hình dạng của chặn tốc độ theo cửa sổ.
  - **Hạn giờ cả lần chạy `CRAWL_DEADLINE_MS` = 11 phút:** hết giờ thì ngừng tải, mã chưa kịp giữ nến cũ, **vẫn ghi file và vẫn commit**. `timeout-minutes` của job nâng 15 → 20 làm mức chặn cuối. Đây là điểm quan trọng nhất: **thà làm mới một nửa còn hơn bị huỷ và không commit gì.**
  - **Thứ tự mã theo độ cũ, VN-Index trước.** Nguồn chặn giữa chừng là chuyện thường (run #6: 45/103 mã), thứ tự cố định thì mã cuối danh sách không bao giờ được làm mới — giờ xếp theo `fetchedAt` tăng dần nên lượt sau tự bù cho lượt trước. VN-Index luôn đầu vì mọi mã chấm điểm so với nó.
  - `CRAWL_IN`/`CRAWL_OUT`/`CRAWL_DRY` cho phép chạy thử không đụng kho thật. Đo trong sandbox (mọi nguồn bị proxy chặn = trường hợp xấu nhất): 26 giây thay vì 15 phút, 102/102 mã giữ nguyên từng nến, lý do lỗi ghi rõ từng mã.
  - **Chạy thật 17/09 22:35 (run #6) sau khi sửa: THÀNH CÔNG.** 45/103 mã lấy mới, 58 mã giữ nến cũ, 646 giây, commit `data: giá VN100 2026-09-17 22:45` lên `main`. VCI vẫn chặn sau ~45 mã (cả 5 nguồn cuối cùng đều bị đánh dấu hỏng) nhưng **job không còn bị huỷ** và kho vẫn được cập nhật — đúng mục đích của hạn giờ. **F11 của audit trả lời xong:** `POST /pages/builds` trả HTTP 201, và bước kiểm chứng đo được **Pages phục vụ kho mới sau 50 giây** — tức là trước đây push bằng GITHUB_TOKEN có thể KHÔNG tự kích Pages, giờ thì chắc chắn.

### Không làm trong P1
State machine, cooldown, crash mode, ACTION_PENDING, ladder/buy zones, decision log, versioning nhiều thuật toán, **chiến lược riêng cho từng coin**, tự động mua, thông báo.

---

## 6. P2 (chưa mở, chỉ ghi để không quên)

Mở khi P1 đã dùng thật ít nhất một tháng. Thứ tự dự kiến: Ladder + buy zones (lưu một document `ladders/active`, anchor cố định, zone không bao giờ dịch lên) → cooldown + override → state machine rút gọn (WAIT / BASE_DUE / SMART_READY / OPPORTUNITY_READY / MONTH_END) → crash mode → ACTION_PENDING. Tất cả vẫn tính lúc mở tab, không có tiến trình nền.

---

## 7. Nhật ký quyết định

| Ngày | Quyết định |
|---|---|
| 2026-09-12 | Bỏ roadmap Finance × CoinDCA và repo `coin`. DCA thành tab nhẹ trong FinTrace. Firebase project mới. Binance trước, CoinGecko dự phòng. P1 = 3 khối C/A/B; state machine, ladder... để P2. |
| 2026-09-13 | Owner yêu cầu theo dõi BTC đầy đủ như ETH. Mở Khối E: tab DCA thành đa coin (ETH/BTC), cùng thuật toán, mỗi coin một ngân sách. "Nhiều coin" ra khỏi mục Không làm; thay bằng "chiến lược riêng cho từng coin". |
| 2026-09-13 | Owner thử trên app thật: Binance chặn CORS endpoint đã ký, endpoint công khai vẫn chạy. Đã hỏi và owner chốt **dựng Cloudflare Worker**. Thêm `binance-worker.js` + `BINANCE.md`; khoá API rời Firestore, chuyển vào biến bí mật của Worker. |
| 2026-09-13 | **Gỡ hẳn đồng bộ số dư Binance.** Worker bị Binance trả 403 (chặn IP trung tâm dữ liệu) — bức tường thứ hai sau CORS. Owner chốt dừng. Đổi lại: nhập tay chỉ nhập số lượng, app tự quy ra nghìn đồng và USD; thêm khoá giá `wbeth` với cặp dự phòng `WBETHETH` vì Binance có thể không có `WBETHUSDT`. |
| 2026-09-13 | Đổi nút chữ (sửa/xoá/Đã nhận/Tất toán/mở lại) trong 4 bảng của tab Dòng tiền thành icon nằm ngay sau tên khoản, cùng dòng với số tiền. Mỗi hàng gộp về một `<td>` để chiều cao hàng bằng nhau ở cả 4 card. |
| 2026-09-13 | Owner phản hồi icon "lộn xộn" (lệch cột do gộp vào một ô flex). Đổi lại: quay về cột `<td>` thật + `table-layout:fixed` + ô giữ chỗ cho icon tuỳ trạng thái — icon giờ thẳng cột tuyệt đối, kể cả xen giữa dòng pending/thường. |
| 2026-09-13 | Gộp 5 ô thống kê Dòng tiền vào một card, đặt cạnh "Chi theo nhóm" phía trên 4 card Chi/Thu/Nợ. "Chi theo nhóm" đổi từ nhiều thanh (mỗi nhóm một dòng) sang một thanh ngang gộp theo tỉ trọng, để hai card tự nhiên cao bằng nhau — không cần giới hạn cuộn. |
| 2026-09-13 | **Sửa giá WBETH sai.** Bỏ cặp trực tiếp `WBETHUSDT` khỏi `LIVE_SYMBOLS`, luôn suy qua tỉ giá neo `WBETHETH`; đổi mọi lệnh ticker sang `bookTicker` (bid/ask) thay vì `ticker/price` (khớp lệnh gần nhất) cho mọi coin. Thêm cột USDT rõ đơn vị trong Bảng giá kèm dòng tỉ suất quy đổi cho giá suy qua tỉ giá neo; xem trước Chuyển đổi tài sản hiện thêm USDT tương đương. Chưa kiểm chứng được với Binance thật do sandbox chặn mạng ra ngoài. |
| 2026-09-13 | **Giá WBETH vẫn bằng ETH — hoá ra là do data, không phải fetch.** Holding WBETH của owner thêm trước khi khoá giá `wbeth` tồn tại nên `priceKey` kẹt ở `eth`; Danh mục không có ô sửa `priceKey` nên owner không tự sửa được. Thêm `fixWbethPriceKey()` tự chạy trong `applyLoaded()` mỗi lần đăng nhập, sửa và ghi lại Firestore không cần thao tác gì thêm. Làm tròn cột "Giá" Danh mục về số nguyên cho gọn. |
| 2026-09-13 | **Danh mục nói bằng USDT cho coin + thêm card "Vùng giá mua".** Cột Giá của dòng coin đổi thành tổng quy ra USDT kèm tỉ giá 1 đơn vị ngay dưới; tài sản khác giữ ô nhập nghìn đồng. Owner hỏi "giá nào là rẻ để mua" ⇒ thêm bảng tra cứu: dò ngược `buyScore()` bằng chia đôi khoảng để ra giá ứng với từng ngưỡng Market State. Không phải ladder P2 — không anchor, không state, tính lại mỗi lần Cập nhật. |
| 2026-09-13 | **Bảng giá bị bỏ sót tỉ giá USDT/VND.** Owner báo lại lần làm trước chỉ thêm dòng tỉ suất neo riêng cho WBETH, thiếu đúng cái được yêu cầu ban đầu: tỉ giá USDT dùng để quy đổi hiện dưới MỌI coin. Thêm dòng `1 USDT ≈ 26,4855k` dưới cột USDT cho mọi dòng có giá, giữ 4 số lẻ (không làm tròn nguyên như cột "Giá" vì mất hết ý nghĩa ở mức tỉ giá này). |
| 2026-09-13 | **Coin dồn hẳn về tab DCA.** Owner muốn mua Base/Smart/Opportunity không cần rời tab DCA, và Danh mục chỉ cần ghi gộp "Coin" thay vì từng dòng. Thêm card "Ví Coin" (bảng từng coin + tỉ trọng, tái dùng `.catstack` của Chi theo nhóm) và "Mua nhanh" (form riêng, đích khoá vào coin đang chọn, nguồn vốn tự điền số còn lại từ `dcaPlanNow()`) — cả hai gọi lại đúng `addTransfer()`/`updateHolding()` nên mọi nơi khác tự cập nhật, không thêm state riêng. Danh mục bớt hẳn nhóm Crypto khỏi form thêm tài sản. |
| 2026-09-13 | **Nạp lịch sử mua coin từ file Excel cũ.** Owner gửi 29 lần mua BTC/ETH/ADA tự ghi tay trước khi có FinTrace — tab DCA chưa có gì để tính giá vốn/giá trung bình. Thêm `DCA_HISTORY_IMPORT` (dữ liệu chép từ file) + `importDcaHistory()`, ghi thẳng `store.batch()` vào `transfers` chứ không qua `addTransfer()` để khỏi cộng dồn nhầm vào số lượng coin đang có (đã đúng số dư thật, độc lập với sổ mua cũ). Tỷ giá VNĐ/USDT từng dòng lấy từ đúng lần nạp USDT gần ngày mua nhất trong cùng file. Một dòng bán (số lượng âm) tự đổi chiều, không tính vào giá vốn. Nút ở Cài đặt → Nâng cao, tự khoá sau khi nạp một lần. |
| 2026-09-13 | **Sửa giá vốn USD tính sai + gộp WBETH vào ETH.** Owner đối chiếu sổ tay: TB giá 2143 mà app ra 2252. Nguyên nhân: lấy giá vốn VNĐ chia tỷ giá USDT **hôm nay** trong khi vốn bỏ ra hồi tỷ giá cao hơn — giờ cộng USD theo tỷ giá từng lần mua. Đồng thời trừ lần bán vào vốn ròng (đúng cách sổ tay tính) và thêm `COIN_FAMILY` để WBETH được tính là ETH đã stake (chuyển ETH↔WBETH là nội bộ, không phải mua/bán). Kết quả khớp đúng sổ tay: $2.142,81. |
| 2026-09-13 | **Tab DCA gom lại một màn + theo dõi ADA và USDT.** Bộ chọn coin vào trong card Giá vốn, card đó lên ngang hàng hero, bỏ ô Giá hiện tại trùng với hero. Vùng giá mua và Tháng này thu thành hai card vuông cạnh nhau. Mua nhanh thành nút mở popup. ADA/USDT thêm vào bộ chọn ở dạng `basisOnly` (chỉ giá vốn). USDT có `fiatOnly` + `pnlRate` vì đem USDT mua coin không phải là bán. Nạp thêm 39 dòng nạp/rút USDT, tỷ giá vốn TB ra 26,9545k đúng sổ tay. |
| 2026-09-13 | **Tab DCA gom về 2 hàng card + 3 popup, cố định kích thước.** Phân rã điểm nhập vào hero; Ví Coin lên hàng đầu bên phải Giá vốn (card này hẹp lại); Lịch sử, Thêm coin, Mua nhanh thành ba nút mở popup. ADA/USDT không giấu card nữa mà ghi "không chấm" trong đúng khung; chiều cao pin bằng min-height đo ở trạng thái đầy nhất ⇒ 5 card giữ nguyên pixel khi đổi coin. Ô thống kê: số và chú thích cùng hàng, cắt gọn, cao cố định. |
| 2026-09-13 | **Gộp Giá vốn + Ví Coin một card, tab DCA vừa một màn hình.** Owner soi ở 1920 thấy các card thừa trống: `min-height` pin đo ở 1440 sai khi nhãn không còn xuống dòng. Bỏ hết pin pixel, chuyển sang ổn định bằng cấu trúc (mọi coin cùng số dòng, điền "—"), bảng nén một dòng/hàng, bố cục 2×2. Đo: 4 card bằng nhau tuyệt đối ở 4 coin, không cuộn ở 1920×860. |
| 2026-09-13 | **Rà soát trùng lặp toàn app (1/3): gộp tab Lịch sử vào Tổng quan, Cài đặt hai cột.** Owner yêu cầu rà mọi tab, gộp thông tin trùng, mọi tab vừa một màn hình, bảng dài thành popup; đã hỏi và owner chốt: Lịch sử gộp vào Tổng quan; Dòng tiền cuộn trong card; Tài sản gộp coin thành "Binance" quy USDT + các nhóm khác thả xuống; Kịch bản thành popup có thanh trượt + biểu đồ. PR này làm phần Tổng quan + Cài đặt. |
| 2026-09-13 | **Rà soát trùng lặp (2/3): Dòng tiền cuộn trong card.** Cả tab cao đúng một màn hình, danh sách Chi/Thu/Nợ cuộn bên trong từng card, form Thêm cố định ở trên. |
| 2026-09-13 | **Rà soát trùng lặp (3/3): Tài sản.** Danh mục thả xuống theo nhóm, coin gộp dòng "Binance" quy USDT, Bảng giá chỉ nhập tay; Chuyển đổi / Lịch sử chuyển đổi / Kịch bản thành popup. Kịch bản có thanh trượt (từng tài sản hoặc cả Crypto), preset từ Vùng giá mua DCA, biểu đồ chiếu 24 tháng đổi ngay khi kéo. Cả 5 tab giờ đều vừa một màn 1920×860. |
| 2026-09-13 | **Rà lại phần "Thị trường" của Phân rã tăng trưởng.** Xác nhận nó lấy đúng giá coin live (qua `D.prices` mà nút Cập nhật DCA ghi vào), nhưng chỉ chạy khi có bản chốt do app tạo — 19 tháng import từ file không có ảnh chụp danh mục nên hiện chưa ra số. Sửa lỗi công nợ tính bằng vàng không được tính vào phần giá (rơi nhầm sang "Khác" và bật cờ cảnh báo sai). |
| 2026-09-13 | **Phân rã tăng trưởng chỉ chạy từ giờ, và chốt tháng muộn không còn đếm trùng.** Owner chốt không dựng lại 19 tháng import (file cũ không có danh mục cuối tháng). Đổi lại, bản chốt lưu ngày chụp thật `snapAsOf` để lần chốt đầu tiên — thường muộn vài ngày — cho ra số đúng ngay, thay vì đẩy phần chênh sang "Khác". |
| 2026-09-13 | **Tự lấy giá Binance và tự chụp danh mục mỗi lần mở app.** Owner hỏi có cách nào nắm tình hình hằng ngày mà không phải bấm Chốt tháng ⇒ thêm collection `snaps` (một ảnh chụp/ngày, giữ 120 ngày) và kỳ **7 ngày / 30 ngày** cho Phân rã tăng trưởng. Chốt tháng nhiều lần đã kiểm chứng là ghi đè, không nhân đôi. |
| 2026-09-17 | **Buổi rà "tăng thu nhập" + mở hướng Trading.** Owner hỏi (1) dùng chỉ báo để trade ngắn hạn được không, (2) cách tăng thu nhập không phổ thông. Trả lời: Buy Score là công cụ định giá 1 năm, dùng nguyên xi cho swing là sai công cụ; bản swing H4 khả thi nhưng phải backtest + paper trước. Owner chốt: **backtest + paper trading trước, khung 3–14 ngày (H4)**; về thu nhập làm **card Chi phí vốn** trước; xác nhận nghề phân phối hàng điện tử. PR này là card Chi phí vốn. Backtest ra khỏi mục "Không làm trong P1" theo yêu cầu owner. |
| 2026-09-17 | **Mở tab Trading (swing H4) ở mức backtest.** Cùng `buyScore()` với cấu hình `SWING_SCORE_V1`, ba hàm thuần backtest, lưới 9 bộ quy tắc so với mua-giữ, tín hiệu hôm nay. Chưa có sổ lệnh giả (PR sau). Trên dữ liệu giả lập, không bộ nào thắng mua-giữ — tab nói thẳng điều đó thay vì tô hồng. |
| 2026-09-17 | **Ẩn Chi phí vốn (nợ thẻ đảo được, không mất lãi) và ra kế hoạch lệnh có số.** Owner hỏi "65 điểm vào bao nhiêu, cao hơn thì sao, thoát khi nào" ⇒ `tradePlan()`: khối lượng theo rủi ro 1/1,5/2% chia cắt lỗ, kẹp 40%, tối đa 2 lệnh, dừng khi sụt 10%/tháng; vốn trade đặt ở Cài đặt. Hướng dẫn vào–ra viết cho người chưa có kinh nghiệm, nhấn mạnh đặt OCO ngay khi vào. |
| 2026-09-17 | **Trading v2: thoát theo bậc thay vì một lần, vào 2 nhịp, backtest 2,5 năm chia hai kỳ.** Backtest thật v1 thua mua-giữ ở cả 9 bộ, hai coin; chẩn đoán: ra-theo-điểm bán đúng lúc sóng bắt đầu, và 5,5 tháng là một trạng thái thị trường. Owner chốt làm và yêu cầu cắt lỗ/chốt lời dần theo tinh thần DCA ⇒ `SWING_RULES_V2` + `simulateLadder`, `fetchKlinesDeep` 5.400 nến, cột Toàn kỳ / 12 th gần, giữ cách cũ làm đối chứng. Sổ lệnh giả vẫn chờ kết quả backtest thật. |
| 2026-09-17 | **Backtest thật v2 vẫn thua ở cả hai coin; owner hỏi futures long/short thay spot.** Trả lời: futures không sửa được "không có lợi thế" (72 lệnh ≈ 14% phí; trước phí hoà, sau phí lỗ), chỉ nhân lên; short ở chỗ đắt là cùng một cược đảo chiều nên vẫn phụ thuộc trạng thái thị trường. Owner chốt **backtest long/short 1x có funding** trước ⇒ v3: điểm short = buyScore trên nến đảo, ba chế độ Spot / L/S 1x / Short 1x cùng lưới, funding giả định bất lợi cả hai chiều. Chưa có gì bằng tiền thật; sổ lệnh giả vẫn chờ có bộ thắng cả hai kỳ. |
| 2026-09-17 | **Backtest thật v3: spot và L/S thua ở cả hai coin; ETH Short 1x +5,2% · +15,4% nhưng đó là lỗi mốc so sánh của app.** Short rồi ôm ETH đã ăn +20,6% / +46,2% (trước funding), chiến lược chỉ bắt được một phần nhỏ với sụt tối đa 33,9%; 2 bộ dương trong 54 ứng viên là mức của may rủi. Sửa mốc theo chiều (`holdReturn`/`tradeBench`). Kết luận giữ nguyên: không tiền thật, tiếp tục DCA; muốn giảm tiếp xúc thì mua chậm lại, không short. |
| 2026-09-17 | **Thêm vỏ iOS (Capacitor) trong `ios-app/`.** Owner có Mac, hỏi làm app iOS; đã đưa ba mức (PWA / Capacitor / native), owner chọn Capacitor. Vỏ tải thẳng Pages nên web vẫn một file không build step; Mac chỉ cần Xcode nhờ SPM. Hướng dẫn thao tác trình duyệt + Xcode trong `IOS.md`. |
| 2026-09-17 | **Mở hướng Chứng khoán VN, PR 1 = dò nguồn dữ liệu.** Owner muốn tab như DCA cho VN30 rồi swing 4H; trả lời: thuật toán dùng lại được, 4H phải thành nến ngày, luật T+2/±7%/không short/phí cao phải mô phỏng đúng, nguồn dữ liệu chưa chắc gọi được từ Pages. Owner chốt dò trước, DCA nhắm từng cổ phiếu theo điểm. Tab `#chungkhoan` thử TCBS/SSI/VNDirect trong trình duyệt và hiện VN30 từ nguồn sống. |
| 2026-09-17 | **Cả 3 nguồn chứng khoán bị chặn CORS trên điện thoại ⇒ chuyển sang crawler GitHub Actions.** Kho `data/vn30.json` cào mỗi ngày 15:40 từ 5 nguồn có dự phòng, Pages phục vụ cùng origin; app đọc kho trước, 3 nguồn trực tiếp chỉ còn dự phòng. Không cần Cloudflare Worker, không có dịch vụ nào phải nuôi ngoài GitHub. |
| 2026-09-17 | **Chứng khoán PR 2: Buy Score nến ngày cho 30 mã VN30, xếp hạng, vùng giá mua, danh mục cổ phiếu có giá tự động.** Bỏ hẳn 3 nguồn gọi thẳng theo yêu cầu owner; kho Actions là nguồn duy nhất, tab tự tải khi mở. `STOCK_SCORE_V1` là điểm xuất phát (ngưỡng ~70% crypto, cửa sổ theo phiên), PR 3 backtest nến ngày với luật T+2/±7%/phí 0,5% sẽ đo thật. |
| 2026-09-17 | **Chứng khoán PR 3: VN100 và backtest swing nến ngày với luật VN.** Kho cào 100 mã (VN70 tham khảo, mã sai sẽ lộ trong errors), bộ chọn VN30/VN100. `simulateLadder` thêm T+2, thuế bán, phiên sàn/trần (mặc định tắt cho coin). Popup Backtest: 9 bộ tổng hợp trên 100 mã + 9 bộ của mã đang chọn + tín hiệu VÀO/CHỜ theo bộ tốt nhất. Kết luận đòi thắng mua-giữ TB cả hai kỳ ở ≥ 60% số mã mới gọi là lợi thế. |
| 2026-09-17 | **Chứng khoán PR 4: nạp 10 lệnh VCBS từ ảnh, danh mục cổ phiếu theo thực tế, bộ "Của tôi".** Lệnh thành transfers tiền ↔ `stk:SYM`, giá vốn bình quân gia quyền + lãi/lỗ hiện thực, khoản VCBS nhập tay đổi thành Tiền tại VCBS ước lượng. Crawler cào thêm DPR, TV2. Ngày 8/10 lệnh là ước lượng (ảnh không mở rộng), ghi rõ trong ghi chú. |
| 2026-09-17 | **Chứng khoán PR 5: mô phỏng kế hoạch "mỗi tháng chọn 2 mã điểm 75+, bán khi điểm về X" + what-if.** Trên kho thật: thắng VN-Index ở 12 tháng gần (+19–25% vs +9%), toàn kỳ chỉ ngưỡng 25 thắng (+63,5% vs +42,7%) và biến thể đó thực chất là giữ 42 phiên; sụt tối đa 26% ở ngưỡng 65/55; 12–17 lệnh là mẫu nhỏ. Bảng what-if tĩnh cho mã đang chọn. Vẫn chưa ghi lệnh thật. |
| 2026-09-17 | **Chứng khoán PR 6: popup Tính PnL kéo điểm + đối chiếu sổ theo ảnh Danh mục VCBS.** Owner muốn "bấm mã ⇒ mua X tiền ở điểm Y, bán ở điểm Z thì ± bao nhiêu" với thanh trượt thay nhập tay ⇒ popup dùng lại `priceForScore`. Đối chiếu: số CP owner sửa tay đúng (trừ VIX 506 → 504) nhưng giá vốn lệch vì cổ phiếu thưởng/quyền không có trong lịch sử lệnh ⇒ thêm transfer `kind:'adjust'` có dấu, nút một lần ở Cài đặt; sổ ra đúng 29.463k / 24.807k / −4.656k như VCBS. |
| 2026-09-17 | **Trading PR 7: gỡ UI swing H4; Trading = bảng theo dõi 100 coin (Binance top 100) + 100 cổ phiếu cùng Buy Score, popup PnL có dòng động, Mua/Bán ghi Chuyển đổi có nhãn trade.** Owner hỏi "PnL động là sao" ⇒ giải thích tĩnh (chỉ giá đổi) vs động (thời gian trôi làm điểm tụt) và làm dòng `scoreAfterFlat`. Tab Cổ phiếu chỉ còn phần đang giữ + lịch sử giao dịch; Cổ phiếu đứng trước Trading. Sổ lệnh đi qua `addTransfer` nên Tài sản/Phân rã tự nhận, Tổng quan thêm dòng ghi chú trading trong kỳ (không cộng thêm vào phân rã). Binance thật chưa gọi được từ sandbox — owner kiểm trên Pages. |
| 2026-09-17 | **Rà soát trùng lặp lần 2.** Hai tab nắm giữ (DCA, Chứng khoán) về cùng một khung 2×2 với cùng bộ nút và cùng số ô; Chứng khoán có bộ chọn mã, 3 ô giá vốn, thanh tỉ trọng và card "Tháng này" (kế hoạch chọn mã theo luật Backtest); Lịch sử giao dịch cổ phiếu thành popup, Mua nhanh dùng chung form Mua/Bán của Trading. Tài sản: nhóm Chứng khoán gộp một dòng như Binance, Lịch sử chuyển đổi thành card chạy hết phần trống thay cho popup. |
| 2026-09-17 | **Sửa trang bị phóng to trên iPhone** (tải trang, chạm đôi, mở popup): ô nhập 16px trên thiết bị cảm ứng, popup không tự focus khi cảm ứng, `touch-action:manipulation`, viewport khoá tỉ lệ cho WKWebView. |
| 2026-09-17 | **Đầu tab đồng nhất:** bỏ dòng giải thích và số đếm ở hàng nút đầu tab, nút cùng cỡ một hàng ở mọi tab (desktop 128px, điện thoại chia đều), bộ chọn Cổ phiếu/Coin của Trading vào card Buy Score. |
| 2026-09-17 | **Xác nhận hồi tách khỏi điểm + nghiên cứu sự kiện.** Owner chỉ ra điểm bão hoà và nghịch lý hồi ⇒ thêm cột Hồi (3 dấu giá/khối lượng, độc lập với điểm) và bảng "Sau khi điểm chạm mốc" trên kho thật: vào ngay rớt thêm trung vị −7,7% (10% xấu −22,5%), chờ xác nhận −0,2% (−12,8%) với lợi suất 63 phiên trung vị +9,3% → +25% nhưng mẫu 235 → 23. Kết luận: dùng làm điều kiện thứ hai khi trade, chưa phải lợi thế đã chứng minh. |
| 2026-09-17 | **Bộ lọc Gợi ý** gom điểm + Hồi + độ tin cậy + độ sâu rớt thành hai hạng mua / theo dõi (loại có lý do), mặc định khi mở Trading; Tháng này dùng cùng bộ lọc. Hôm nay 0 mua, 9 theo dõi. |
| 2026-09-17 | **Nhãn rẻ kiểu gì (cấu trúc / hoảng loạn / hỗn hợp) + sự kiện tách theo nhãn.** Trên kho thật: hoảng loạn vào ngay 90% dương · +24,4% sau 63 phiên (42 sự kiện); cấu trúc vào ngay 57% · +2,5% (91), chờ xác nhận mới lên 83% · +26,1% (11). Nhãn chỉ hiện, chưa thành điều kiện — bước 3 chờ owner. |
| 2026-09-17 | **Bước 3: nhãn thành điều kiện vào (hoảng loạn vào ngay, cấu trúc/hỗn hợp cần đã chạm 75 trong 21 phiên + Hồi ≥ 2/3) và so ba cách vào.** Trên kho thật luật mới chỉ ra 3–4 lệnh trong 2,3 năm vì kế hoạch mua đầu tháng lệch nhịp với dấu hồi; sụt tối đa 5–7% so 26% của luật cũ nhưng mẫu quá nhỏ để nói lãi. Chưa kết luận luật mới hơn luật cũ; bước kế là cho kế hoạch soi từng phiên. Hôm nay vẫn 0 mua, 20 theo dõi (toàn cấu trúc). |
| 2026-09-17 | **Sửa theo audit ngoài (GPT6 Astra).** Owner bỏ F01 (dữ liệu cá nhân trong repo), ưu tiên phần chỉ số. Đã sửa: chỉ chấm nến đã đóng (coin), tuổi dữ liệu từng mã vào Confidence, một hàm quyết định chung cho Gợi ý và backtest, khớp giá mở phiên sau + luật sàn/trần + vị thế mở không ép bán, sổ lệnh không bịa giá vốn, cache backtest theo kho, bảng sự kiện hiện n từng ô, nhịp 2 của ladder không dùng tiền chốt lời, parser kho lọc/sắp xếp. Kết quả thật sau sửa: kế hoạch cũ X=25 từ +63,5% còn +42,2%; điểm + Hồi soi từng phiên 10 lệnh 90% +14,6% sụt 5,2%; theo kiểu rẻ không hơn điểm + Hồi. |
| 2026-09-17 | **Audit đợt 2 — làm nốt phần đúng còn treo.** Bậc TP/SL theo đáy/đỉnh trong phiên + vào ở giá mở phiên sau cho lưới; kiểm định ngoài mẫu chia đôi kỳ; nhãn Độ tin cậy → Dữ liệu; DCA không mở Smart/Opp trên điểm cũ; workflow gọi Pages build và kiểm chứng kho mới; kho coin kèm coin đang giữ; form Mua chặn vượt số dư, cảnh báo luật vốn trade; bảng sự kiện ghi số bỏ qua và chi phí chờ. Ngoài mẫu: luật chọn theo nửa đầu thua ở nửa sau (kế hoạch −17,7% khi VN-Index +22,7%) ⇒ chưa luật nào đáng tiền thật. |
| 2026-09-17 | **Thêm `HUONGDAN.md` — tài liệu cho người dùng cuối.** Owner định giao trợ lý ghi thu chi hằng ngày ⇒ viết hướng dẫn theo thao tác: quy ước nghìn đồng, ghi chi/thu/nợ từng ô, ý nghĩa 4 icon, danh sách việc trợ lý KHÔNG được tự bấm (chốt tháng, khôi phục, sửa tài sản, mua bán), sự cố thường gặp, bảng tra nhanh. Link mở trong app ở Cài đặt → Tài khoản. Chạy Actions cào giá VN100 theo yêu cầu owner. |
| 2026-09-17 | **Sửa crawler bị timeout hai lần liên tiếp.** Nguyên nhân: VCI chặn tốc độ sau ~40 mã, bốn nguồn dự phòng đều chết nên mỗi mã hỏng tốn 33 giây, job bị huỷ ở phút 15 nên không commit gì. Sửa: chỉ xin phần nến thiếu thay vì 800 mỗi ngày, thử lại VCI 3 lần timeout 8 giây, bỏ nguồn đã hỏng 5 lần, nghỉ 45 giây khi hỏng 8 mã liên tiếp, và hạn giờ 11 phút để luôn commit phần đã làm được. |
| 2026-09-13 | **Gộp Sổ tháng + Công nợ thành tab Dòng tiền.** Bốn card ngang chia đều. Công nợ giữ nguyên là số dư hôm nay dù xem tháng nào. Bỏ trùng lặp "thu chưa nhận". Nhóm chi "Trả nợ" giữ nguyên như ghi chú. Ngưỡng bố cục đo thật chứ không đoán. Thêm `testViews()`. |
| 2026-09-13 | **Sự cố mất dữ liệu.** Owner khôi phục file cũ lên Firebase và mất dữ liệu cả tháng 9. Firestore không có đường hoàn tác: `getFirestore()` mặc định là cache trong RAM nên không còn bản sao trong IndexedDB, và PITR phải bật trước (Blaze). Hai việc đã làm: (1) nút **Tìm dữ liệu cũ trong trình duyệt này** đọc lại `localStorage` còn sót từ thời chế độ thử cục bộ — chế độ Firebase không bao giờ đụng vào chỗ đó; (2) `restoreBackup` và `importInitialData({force})` giờ **tự tải một bản sao lưu về máy trước khi gọi `clearAll()`**. Mọi thao tác xoá sạch về sau phải giữ chốt này. |
